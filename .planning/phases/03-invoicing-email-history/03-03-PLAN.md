---
phase: 03-invoicing-email-history
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - lib/invoice/mailto.ts
  - lib/actions/invoices.ts
  - components/invoice-delete-confirm-dialog.tsx
  - components/invoice-view.tsx
autonomous: true
requirements: [MAIL-01, MAIL-02, MAIL-04]
must_haves:
  truths:
    - "On the invoice view, the tutor clicks Email Invoice and a Gmail compose tab opens pre-filled with the parent's email as recipient, the frozen subject, and the frozen body."
    - "When the built Gmail URL would exceed ~1,800 chars, the Email button is hidden and an inline message directs the tutor to the always-present Copy button instead."
    - "On the invoice view, the tutor can Delete Invoice; confirming atomically deletes the invoice AND returns its sessions to unbilled (they reappear on the Dashboard), then navigates back to /history."
  artifacts:
    - path: "lib/invoice/mailto.ts"
      provides: "buildGmailComposeUrl + over-length helpers"
      exports: ["buildGmailComposeUrl", "isGmailUrlTooLong", "GMAIL_URL_MAX_LEN"]
    - path: "lib/actions/invoices.ts"
      provides: "deleteInvoiceAction (atomic un-bill + delete)"
      exports: ["deleteInvoiceAction"]
    - path: "components/invoice-delete-confirm-dialog.tsx"
      provides: "delete-invoice confirm dialog (bound Server Action)"
    - path: "components/invoice-view.tsx"
      provides: "Email Invoice button + over-length guard + Delete Invoice trigger (modified)"
  key_links:
    - from: "components/invoice-view.tsx"
      to: "buildGmailComposeUrl"
      via: "Email Invoice anchor href"
      pattern: "buildGmailComposeUrl"
    - from: "deleteInvoiceAction"
      to: "db.batch"
      via: "un-bill sessions + delete invoice atomically"
      pattern: "db\\.batch"
    - from: "components/invoice-delete-confirm-dialog.tsx"
      to: "deleteInvoiceAction"
      via: "bound Server Action on submit"
      pattern: "deleteInvoiceAction\\.bind"
---

<objective>
Upgrade the invoice view's send path to one-click Gmail (D-10) and add the delete +
un-bill mistake-recovery escape hatch (D-16). After Plan 02 the tutor can already send by
copy/paste; this plan makes the primary path a pre-filled Gmail compose draft to the
parent, with a graceful over-length guard that falls back to the always-present Copy
button. It also lets a wrong invoice be fully deleted, returning its sessions to the
Dashboard for regeneration.

Purpose: Completes the "sessions → parent's inbox in a couple clicks" core value and the
only supported recovery flow.
Output: `lib/invoice/mailto.ts`, `deleteInvoiceAction` (db.batch), the delete-confirm
dialog, and the Email/Delete additions to the shared invoice view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-invoicing-email-history/03-CONTEXT.md
@.planning/phases/03-invoicing-email-history/03-RESEARCH.md
@.planning/phases/03-invoicing-email-history/03-PATTERNS.md
@.planning/phases/03-invoicing-email-history/03-UI-SPEC.md
@.planning/phases/03-invoicing-email-history/03-02-PLAN.md
</context>

## Artifacts this phase produces (Plan 03)

New symbols/paths (excluded from source-grounding drift checks):
- `lib/invoice/mailto.ts` — `buildGmailComposeUrl`, `isGmailUrlTooLong`, `GMAIL_URL_MAX_LEN`
- `lib/actions/invoices.ts` — adds `deleteInvoiceAction`
- `components/invoice-delete-confirm-dialog.tsx` — `InvoiceDeleteConfirmDialog`
- `components/invoice-view.tsx` — adds Email Invoice button, over-length guard message, Delete Invoice trigger

<tasks>

<task type="auto">
  <name>Task 1: Gmail compose URL builder + over-length guard</name>
  <files>lib/invoice/mailto.ts</files>
  <read_first>
    - lib/format.ts (pure-function, no-side-effect shape to mirror)
    - .planning/phases/03-invoicing-email-history/03-RESEARCH.md (Code Examples: Gmail compose URL via URLSearchParams; Pitfall 5 length threshold; Pitfall 6 encoding; D-10 locked view=cm&fs=1 format)
    - .planning/phases/03-invoicing-email-history/03-UI-SPEC.md (Copywriting Contract over-length guard; ~1,800 char threshold)
  </read_first>
  <action>
    `lib/invoice/mailto.ts` — pure functions, no React/DB import. Export:
    - `GMAIL_URL_MAX_LEN = 1800` (conservative safety margin, RESEARCH Pitfall 5 / A1).
    - `buildGmailComposeUrl({ to, subject, body }: { to: string; subject: string; body:
      string }): string` — build with `URLSearchParams({ view: "cm", fs: "1", to, su: subject,
      body })` (D-10 locked param set) and return `\`https://mail.google.com/mail/?${query.toString()}\``.
      Rely on URLSearchParams for encoding — NEVER manual string concatenation (Pitfall 6:
      encodes `&`/`#`/newlines in notes so a stray char can't inject query params or truncate).
    - `isGmailUrlTooLong(url: string): boolean` — `url.length > GMAIL_URL_MAX_LEN`.
    Add a comment noting `view=cm&fs=1` is the D-10-locked format and `tf=cm` is the documented
    fallback to try if manual testing shows it stops opening a compose window (RESEARCH State of
    the Art / A3) — do not implement the fallback now.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
    <automated>grep -q "URLSearchParams" lib/invoice/mailto.ts && grep -q "view" lib/invoice/mailto.ts && grep -q "GMAIL_URL_MAX_LEN" lib/invoice/mailto.ts && ! grep -q 'mail.google.com/mail/?view=cm&fs=1&to=' lib/invoice/mailto.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - buildGmailComposeUrl uses URLSearchParams (no manual `+`/`&` string concatenation of the params).
    - GMAIL_URL_MAX_LEN === 1800; isGmailUrlTooLong compares against it.
    - `npx tsc --noEmit` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>A pure, correctly-encoded Gmail compose URL builder + length guard exist.</done>
</task>

<task type="auto">
  <name>Task 2: deleteInvoiceAction (atomic un-bill + delete) + delete-confirm dialog</name>
  <files>lib/actions/invoices.ts, components/invoice-delete-confirm-dialog.tsx</files>
  <read_first>
    - lib/actions/invoices.ts (the file being modified — imports, InvoiceActionState, generate action for context)
    - lib/actions/sessions.ts (deleteSessionAction bound-arg id-guard idiom)
    - components/session-delete-confirm-dialog.tsx (clone near-verbatim: Dialog shell, bound Server Action on submit, trigger-neutral/confirm-red rule)
    - lib/db/schema.ts (sessions.invoiceId + invoices)
    - .planning/phases/03-invoicing-email-history/03-RESEARCH.md (Architecture Diagram delete batch; Pitfall 2 NEVER db.transaction)
    - .planning/phases/03-invoicing-email-history/03-UI-SPEC.md (Surface 4/5 delete only from invoice view, navigate to /history; Copywriting Contract delete rows)
  </read_first>
  <action>
    `lib/actions/invoices.ts` (ADD to the existing file): export
    `deleteInvoiceAction(id: number): Promise<void>`. Guard: `const invoiceId = Number(id);
    if (!Number.isInteger(invoiceId) || invoiceId <= 0) throw new Error("Invalid invoice id.")`
    (mirrors deleteSessionAction). Atomically un-bill + delete via
    `await db.batch([ db.update(sessions).set({ billed: false, invoiceId: null })
    .where(eq(sessions.invoiceId, invoiceId)), db.delete(invoices).where(eq(invoices.id,
    invoiceId)) ])` — `db.batch`, NEVER `db.transaction` (Pitfall 2). Then
    `revalidatePath("/dashboard")` + `revalidatePath("/history")` and
    `redirect("/history")` (import `redirect` from "next/navigation" — Server Action
    throws-to-navigate, so no client router plumbing needed; this is why the action returns
    void, unlike generate which needed to return the new id). Import `and`/`inArray` are not
    needed here; keep imports minimal (`eq`, `redirect`, `revalidatePath`, `db`, tables).

    `components/invoice-delete-confirm-dialog.tsx` ("use client"): clone
    session-delete-confirm-dialog.tsx. Props: `invoiceId`, `studentName`, `period`,
    `sessionCount`, optional trigger variant/size. Trigger neutral outline labeled "Delete
    Invoice". Dialog title "Delete this invoice?" (UI-SPEC). Body per Copywriting Contract:
    "This will permanently delete the invoice for {studentName} ({period}) and return its
    {sessionCount} session(s) to unbilled. This cannot be undone." Form
    `action={deleteInvoiceAction.bind(null, invoiceId)} onSubmit={() => setOpen(false)}`.
    Footer: "Keep Invoice" (DialogClose outline) + "Delete Invoice" (red
    `bg-red-600 text-white hover:bg-red-700`). Because the action calls `redirect("/history")`,
    no client-side navigation is needed here.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
    <automated>grep -q "deleteInvoiceAction" lib/actions/invoices.ts && grep -q "db.batch" lib/actions/invoices.ts && grep -q "redirect(\"/history\")" lib/actions/invoices.ts && ! grep -q "db.transaction" lib/actions/invoices.ts && echo DELETE_OK</automated>
    <automated>grep -q "deleteInvoiceAction.bind" components/invoice-delete-confirm-dialog.tsx && echo BOUND_OK</automated>
  </verify>
  <acceptance_criteria>
    - deleteInvoiceAction un-bills via `db.batch` (billed:false + invoiceId:null) then deletes the invoice; no `db.transaction`.
    - Positive-integer id guard present before any DB write.
    - Action redirects to /history server-side.
    - Confirm dialog uses the bound-Server-Action-on-submit pattern with trigger-neutral / confirm-red colors.
  </acceptance_criteria>
  <done>A wrong invoice can be deleted, returning all its sessions to unbilled, landing back on History.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Email Invoice + over-length guard + Delete trigger into the invoice view</name>
  <files>components/invoice-view.tsx</files>
  <read_first>
    - components/invoice-view.tsx (the file being modified — Plan 02 left a "Plan 03" comment marking where Email + Delete slot in; props already include parentEmail/renderedSubject/renderedBody/studentName/period/sessionCount)
    - lib/invoice/mailto.ts (Task 1 exports)
    - components/invoice-delete-confirm-dialog.tsx (Task 2)
    - components/ui/button.tsx (Button variants for the anchor-styled Email button)
    - .planning/phases/03-invoicing-email-history/03-UI-SPEC.md (Surface 4 action row: Email accent anchor / Copy outline / Delete outline trigger; over-length guard copy)
  </read_first>
  <action>
    Modify `components/invoice-view.tsx`'s action row (where Plan 02 left the placeholder
    comment). Compute the Gmail URL once: `const gmailUrl = buildGmailComposeUrl({ to:
    parentEmail, subject: renderedSubject, body: renderedBody })` and `const tooLong =
    isGmailUrlTooLong(gmailUrl)`.
    - When NOT tooLong: render "Email Invoice" as an ACCENT anchor styled as a Button —
      `<a target="_blank" rel="noopener noreferrer" href={gmailUrl}>` with accent classes
      (`bg-blue-600 text-white hover:bg-blue-700`). (D-10, MAIL-01/MAIL-02.)
    - When tooLong: OMIT the Email anchor entirely and render, above the Copy button, the
      guard message `<p className="text-sm text-zinc-600">This invoice is too long for a
      pre-filled email draft — copy the text below and paste it into a new email.</p>`
      (UI-SPEC over-length guard, Pitfall 5). The Copy button (already present from Plan 02) is
      always shown regardless.
    - Keep the existing Copy Invoice Text button.
    - Add `<InvoiceDeleteConfirmDialog invoiceId={invoiceId} studentName={studentName}
      period={formatPeriod(periodStart, periodEnd)} sessionCount={sessionCount} />` as the
      third action (outline trigger). This is the ONLY delete entry point (Surface 4 — not
      duplicated on the History row).
    MAIL-04 note: parent email is guaranteed present (P1 D-13 required+unique) — no no-email
    guard UI is built (per CONTEXT Deferred Ideas); `to` is always a real recipient.
    Do not introduce `dangerouslySetInnerHTML` (T-03-07 still applies).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
    <automated>grep -q "buildGmailComposeUrl" components/invoice-view.tsx && grep -q "isGmailUrlTooLong" components/invoice-view.tsx && grep -q "InvoiceDeleteConfirmDialog" components/invoice-view.tsx && grep -q 'target="_blank"' components/invoice-view.tsx && ! grep -q "dangerouslySetInnerHTML" components/invoice-view.tsx && echo VIEW_OK</automated>
    <human-check>Open a generated invoice. Click Email Invoice: a Gmail compose tab opens in a new tab pre-filled with the parent's email as To, the frozen subject, and the frozen body (line breaks intact). Then delete that invoice from the view, confirm, and verify you land on /history and the student's sessions are back as unbilled on the Dashboard (billed total restored). If you have (or fabricate) a very long invoice, confirm the Email button is replaced by the over-length message while Copy still works.</human-check>
  </verify>
  <acceptance_criteria>
    - Email Invoice is an `<a target="_blank" rel="noopener noreferrer">` built from buildGmailComposeUrl (source assertion).
    - Over-length branch hides the Email anchor and shows the guard message; Copy stays present.
    - Delete Invoice trigger renders InvoiceDeleteConfirmDialog (the sole delete entry point).
    - No `dangerouslySetInnerHTML`; `npm run build` succeeds.
  </acceptance_criteria>
  <done>The invoice view offers one-click Gmail send (with over-length fallback) and delete+un-bill recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text (notes/zelle/name) → Gmail URL params | Unescaped input could inject/truncate the URL |
| delete-confirm form → deleteInvoiceAction | id crosses the Server Action boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Tampering | Gmail URL construction | mitigate | Build via URLSearchParams/encodeURIComponent only — a stray `&`/`#`/newline in a note cannot inject extra params or break the URL (Pitfall 6) |
| T-03-10 | Tampering | silent Gmail URL truncation | mitigate | isGmailUrlTooLong length check hides Email + forces the always-present Copy fallback (Pitfall 5) |
| T-03-11 | Integrity | deleteInvoiceAction un-bill + delete | mitigate | `db.batch` atomic (both or neither); positive-int id guard; NEVER db.transaction (Pitfall 2) |
| T-03-12 | Tampering (open redirect / tab) | Email anchor target=_blank | mitigate | `rel="noopener noreferrer"`; href is an app-built Gmail URL, not user-controlled destination |
| T-03-13 | Denial (moot guard) | MAIL-04 no-email case | accept | Parent email required+unique (P1 D-13) — recipient always present; no guard UI per Deferred Ideas |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` pass.
- Source assertions: URLSearchParams-built URL, `db.batch` (no `db.transaction`), `rel="noopener noreferrer"`, no `dangerouslySetInnerHTML`.
- End-of-phase human-check: Gmail draft opens correctly; delete returns sessions to unbilled; over-length falls back to Copy.
</verification>

<success_criteria>
- MAIL-01: pre-filled Gmail draft opens addressed to the parent.
- MAIL-02: draft body = frozen invoice summary + Zelle instructions from the template.
- MAIL-04: recipient always present (P1 D-13) — no build needed; documented as a satisfied invariant.
- D-16: delete + un-bill recovery works atomically.
</success_criteria>

<output>
Create `.planning/phases/03-invoicing-email-history/03-03-SUMMARY.md` when done.
</output>
