---
phase: 03-invoicing-email-history
plan: 04
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - components/invoice-history-table.tsx
  - app/(app)/history/page.tsx
  - components/session-form-dialog.tsx
autonomous: true
requirements: [HIST-01, HIST-02]
must_haves:
  truths:
    - "The tutor can open History from the top nav and see a flat, newest-first list of every generated invoice showing student, period, total, and generated date."
    - "Each history row has a View action that opens that invoice's frozen snapshot (the same invoice view built in Plan 02) at /history/[id]."
    - "When no invoices exist yet, History shows an empty state with a Go to Dashboard link."
    - "The Notes field in the Log/Edit Session dialog shows a hint that notes appear on invoices sent to parents (D-02)."
  artifacts:
    - path: "components/invoice-history-table.tsx"
      provides: "responsive table(md+)/cards(mobile) invoice list + empty state"
    - path: "app/(app)/history/page.tsx"
      provides: "Server Component reading invoices newest-first joined to students"
    - path: "components/session-form-dialog.tsx"
      provides: "notes-are-parent-facing hint (modified)"
  key_links:
    - from: "app/(app)/history/page.tsx"
      to: "invoices table"
      via: "db.select orderBy desc(generatedAt)"
      pattern: "desc\\(invoices\\.generatedAt\\)"
    - from: "components/invoice-history-table.tsx"
      to: "/history/[id]"
      via: "View link per row"
      pattern: "/history/"
---

<objective>
Deliver the Invoice History slice (HIST-01, HIST-02): a flat, newest-first log of every
generated invoice, each openable to its frozen snapshot via the shared invoice view built
in Plan 02. Also add the small D-02 labeling change that warns the tutor her session notes
now appear on parent-facing invoices.

Purpose: Closes the loop — the tutor can look back at everything she's billed and reopen
any invoice to re-send or re-copy (the invoice view already carries those affordances).
Output: `components/invoice-history-table.tsx`, `app/(app)/history/page.tsx`, and the
notes hint in `session-form-dialog.tsx`.

This plan runs in Wave 3 in parallel with Plan 03 — it shares no files with Plan 03 and
depends only on Plan 02's invoice view + `/history/[id]` route + invoices table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-invoicing-email-history/03-CONTEXT.md
@.planning/phases/03-invoicing-email-history/03-RESEARCH.md
@.planning/phases/03-invoicing-email-history/03-PATTERNS.md
@.planning/phases/03-invoicing-email-history/03-UI-SPEC.md
@.planning/phases/03-invoicing-email-history/03-02-PLAN.md
</context>

## Artifacts this phase produces (Plan 04)

New symbols/paths (excluded from source-grounding drift checks):
- `components/invoice-history-table.tsx` — `InvoiceHistoryTable`
- Route `app/(app)/history/page.tsx` — `/history`
- `components/session-form-dialog.tsx` — adds the "Notes appear on invoices sent to parents." hint line

<tasks>

<task type="auto">
  <name>Task 1: Invoice History list table + /history route</name>
  <files>components/invoice-history-table.tsx, app/(app)/history/page.tsx</files>
  <read_first>
    - components/student-table.tsx (clone the responsive table(md+)/cards(mobile) shell + empty-state block verbatim; emptyState.action convention)
    - app/(app)/dashboard/page.tsx (Server Component read + container classes)
    - app/(app)/sessions/page.tsx (leftJoin students idiom for the name column)
    - lib/invoice/render.ts (formatPeriod for the Period column)
    - lib/format.ts (formatCents for the Total column)
    - lib/db/schema.ts (invoices columns)
    - components/ui/button.tsx (outline "View" button/Link)
    - .planning/phases/03-invoicing-email-history/03-UI-SPEC.md (Surface 5 columns/mobile card/empty state; Copywriting Contract History rows)
  </read_first>
  <action>
    `components/invoice-history-table.tsx` (Server Component — no "use client"; it renders
    rows + a `Link` per row, no interactivity): clone student-table.tsx's exact
    `hidden md:block` table / `flex flex-col gap-3 md:hidden` cards split + the `py-16` empty
    state. Props: an array of `{ id, studentName, periodStart, periodEnd, totalCents,
    generatedAt }`. Columns (Surface 5): Student | Period | Total | Generated | Actions
    (right-aligned). Period cell = `formatPeriod(periodStart, periodEnd)`; Total =
    `formatCents(totalCents)`; Generated = `format(generatedAt, "PPP")` (import `format` from
    "date-fns"). Actions cell = a single outline "View" `Link` to `/history/{id}` styled as a
    Button (`buttonVariants`/Button render pattern used elsewhere — match how existing
    outline links are styled). Mobile card: student name `font-medium` + "View" on the first
    `justify-between` line, then Period / Total / Generated as three `text-base text-zinc-600`
    lines. Empty state (zero invoices): heading "No invoices yet", body "Generate your first
    invoice from the Dashboard to see it here.", action = an outline "Go to Dashboard" `Link`
    to `/dashboard` (mirror StudentTable's emptyState.action).

    `app/(app)/history/page.tsx` (Server Component): container `mx-auto w-full max-w-3xl px-4
    py-8 sm:py-12`, `<h1 ...>Invoice History</h1>` (28px/600 — nav label stays "History").
    Read newest-first: `db.select({ id: invoices.id, studentName: students.name, periodStart:
    invoices.periodStart, periodEnd: invoices.periodEnd, totalCents: invoices.totalCents,
    generatedAt: invoices.generatedAt }).from(invoices).leftJoin(students,
    eq(invoices.studentId, students.id)).orderBy(desc(invoices.generatedAt))` (D-14 flat,
    newest-first; HIST-01). Pass rows into `<InvoiceHistoryTable rows={...} />`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
    <automated>grep -q "desc(invoices.generatedAt)" 'app/(app)/history/page.tsx' && grep -q "from(invoices)" 'app/(app)/history/page.tsx' && grep -q "/history/" components/invoice-history-table.tsx && grep -q "No invoices yet" components/invoice-history-table.tsx && echo OK</automated>
    <human-check>Generate two invoices for different students. Visit History from the nav: both appear newest-first with Student / Period / Total / Generated columns; click View on one and confirm it opens that invoice's frozen snapshot at /history/[id] (with the Email/Copy/Delete affordances from earlier plans). Delete all invoices and confirm the empty state with the Go to Dashboard link renders.</human-check>
  </verify>
  <acceptance_criteria>
    - history/page reads from `invoices` ordered by `desc(invoices.generatedAt)` (source assertion — D-14/HIST-01).
    - Each row's View links to `/history/{id}` (HIST-02 — reuses the Plan 02 invoice view).
    - Responsive table(md+)/cards(mobile) + empty state cloned from StudentTable.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>A flat newest-first invoice log exists; any row opens its frozen snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Notes-are-parent-facing hint in the session dialog</name>
  <files>components/session-form-dialog.tsx</files>
  <read_first>
    - components/session-form-dialog.tsx (the file being modified — the Notes field block: Label "Notes (optional)" + Textarea)
    - .planning/phases/03-invoicing-email-history/03-UI-SPEC.md (Surface 7 exact hint placement + copy; Copywriting Contract notes-hint row)
  </read_first>
  <action>
    In `components/session-form-dialog.tsx`, inside the Notes field block, insert ONE line
    between the existing "Notes (optional)" `Label` and the `Textarea`:
    `<p className="text-sm text-zinc-600">Notes appear on invoices sent to parents.</p>`
    (D-02, Surface 7). This is a labeling-only change — the field stays labeled "Notes
    (optional)", same 3-row Textarea, same placeholder, same `name="notes"`. Applies to both
    add and edit modes since they share this one component. No schema change, no validation
    change.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
    <automated>grep -q "Notes appear on invoices sent to parents." components/session-form-dialog.tsx && echo HINT_OK</automated>
  </verify>
  <acceptance_criteria>
    - The exact hint string "Notes appear on invoices sent to parents." renders between the Notes label and textarea (source assertion).
    - No change to the Notes field name/rows/optional labeling; no validation/schema change.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>The tutor is warned that session notes are now parent-facing before she writes them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated route access | /history exposes past invoices (parent emails, amounts) |
| history read → invoice view | Reads only frozen columns; no re-derivation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Information Disclosure | /history list | accept | Global Phase 1 middleware gate auto-covers /history; no matcher change added |
| T-03-15 | Tampering (immutability) | history → invoice view | mitigate | View opens the Plan 02 route that reads only frozen invoice columns, never live sessions (INV-04) |
| T-03-16 | Tampering (Stored XSS) | student name / period in list | mitigate | All cells are plain React text nodes; no `dangerouslySetInnerHTML` |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` pass.
- Source assertions: history read ordered by `desc(generatedAt)`, View links to /history/[id], notes hint string present.
- End-of-phase human-check: history lists invoices newest-first; View opens the frozen snapshot; empty state renders; notes hint visible in the session dialog.
</verification>

<success_criteria>
- HIST-01: a log of all previously generated invoices (flat, newest-first).
- HIST-02: opening a row shows its frozen snapshot (student, sessions, total, generated date).
- D-02: notes-are-parent-facing hint surfaced in the session form.
</success_criteria>

<output>
Create `.planning/phases/03-invoicing-email-history/03-04-SUMMARY.md` when done.
</output>
