---
phase: 03-invoicing-email-history
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/invoice/render.ts
  - lib/validation/invoice.ts
  - lib/actions/invoices.ts
  - components/invoice-preview-dialog.tsx
  - components/dashboard-table.tsx
  - components/invoice-view.tsx
  - app/(app)/history/[id]/page.tsx
autonomous: true
requirements: [INV-01, INV-02, INV-03, INV-04, MAIL-03]
must_haves:
  truths:
    - "From a dashboard row with unbilled sessions, the tutor clicks Generate Invoice, previews the exact rendered text, and confirms Generate & Freeze."
    - "Generating atomically inserts an immutable invoice snapshot AND marks that student's unbilled sessions billed=true with invoiceId set — the dashboard unbilled total for that student drops to $0."
    - "After generating, the tutor lands on the finished invoice view (/history/[id]) showing the frozen invoice text."
    - "The invoice view has a Copy Invoice Text button that copies the frozen body to the clipboard (with an auto-selected textarea fallback on clipboard failure)."
    - "Editing or deleting a billed session afterward never changes the frozen invoice text, total, or line items."
  artifacts:
    - path: "lib/invoice/render.ts"
      provides: "buildLineItems, formatPeriod, renderInvoiceText, renderTemplate pure functions + InvoiceLineItem type"
      exports: ["buildLineItems", "formatPeriod", "renderInvoiceText", "renderTemplate", "InvoiceLineItem"]
    - path: "lib/actions/invoices.ts"
      provides: "generateInvoiceAction (atomic snapshot + bill)"
      exports: ["generateInvoiceAction", "InvoiceActionState"]
    - path: "components/invoice-preview-dialog.tsx"
      provides: "D-07 preview-then-confirm modal that navigates to the invoice on success"
    - path: "components/invoice-view.tsx"
      provides: "shared frozen-snapshot view + Copy Invoice Text (reused by History in Plan 04)"
    - path: "app/(app)/history/[id]/page.tsx"
      provides: "invoice detail route (post-generate landing + History open target)"
  key_links:
    - from: "components/dashboard-table.tsx"
      to: "InvoicePreviewDialog"
      via: "per-row Generate Invoice trigger"
      pattern: "InvoicePreviewDialog"
    - from: "generateInvoiceAction"
      to: "db.batch"
      via: "atomic bill-sessions after insert-returning-id"
      pattern: "db\\.batch"
    - from: "components/invoice-preview-dialog.tsx"
      to: "/history/"
      via: "router.push on success"
      pattern: "router\\.push"
    - from: "app/(app)/history/[id]/page.tsx"
      to: "invoices table"
      via: "db.select frozen row"
      pattern: "from\\(invoices\\)"
---

<objective>
Deliver the "money shot" core: generate a student's invoice from the Dashboard,
preview the exact rendered text, confirm to atomically freeze an immutable snapshot +
mark the sessions billed, and land on the finished invoice view where the tutor can copy
the text (and paste it into any email). This is a fully usable end-to-end slice —
sessions → frozen invoice → copyable text — even before the one-click Gmail draft (Plan
03) is added.

Purpose: Proves INV-01..04 (the phase's highest-risk atomicity + immutability
requirements) and MAIL-03 (copy fallback), which is the guaranteed send path.
Output: `lib/invoice/render.ts`, `generateInvoiceAction` (db.batch, NOT db.transaction),
the preview modal, the Dashboard trigger, the shared invoice view, and the
`/history/[id]` route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-invoicing-email-history/03-CONTEXT.md
@.planning/phases/03-invoicing-email-history/03-RESEARCH.md
@.planning/phases/03-invoicing-email-history/03-PATTERNS.md
@.planning/phases/03-invoicing-email-history/03-UI-SPEC.md
@.planning/phases/03-invoicing-email-history/03-01-PLAN.md
</context>

## Artifacts this phase produces (Plan 02)

New symbols/paths (excluded from source-grounding drift checks):
- `lib/invoice/render.ts` — `InvoiceLineItem` type, `buildLineItems`, `formatPeriod`, `renderInvoiceText`, `renderTemplate`, `MERGE_FIELDS`
- `lib/validation/invoice.ts` — `invoiceGenerateSchema` (studentId)
- `lib/actions/invoices.ts` — `generateInvoiceAction`, `InvoiceActionState` (`{ fieldErrors, invoiceId }`)
- `components/invoice-preview-dialog.tsx` — `InvoicePreviewDialog`
- `components/invoice-view.tsx` — `InvoiceView` (props include studentName, parentEmail, period, total, generatedAt, renderedSubject, renderedBody, sessionCount — the extra fields are wired now so Plan 03 only edits this component, not the page)
- Route `app/(app)/history/[id]/page.tsx` — `/history/[id]`

<tasks>

<task type="auto">
  <name>Task 1: Invoice-text pure functions + generate validation</name>
  <files>lib/invoice/render.ts, lib/validation/invoice.ts</files>
  <read_first>
    - lib/format.ts (formatCents + formatDuration — the SINGLE source of truth; render.ts MUST import + reuse these, never reimplement)
    - components/dashboard-table.tsx (local `formatSessionDate` — the `format(new Date(\`${date}T00:00:00\`), "PPP")` display-only date idiom to copy)
    - lib/validation/session.ts (createInsertSchema + z.coerce override idiom)
    - .planning/phases/03-invoicing-email-history/03-RESEARCH.md (Pattern 3 renderTemplate; Code Examples)
    - .planning/phases/03-invoicing-email-history/03-UI-SPEC.md (Data Formatting Conventions: line item, header, total, period label, merge fields — the exact string shapes)
  </read_first>
  <action>
    `lib/invoice/render.ts` — pure functions, NO React/DB import (doc-comment it as the
    invoice-text single source of truth, mirroring lib/format.ts's convention). Import
    `formatCents`, `formatDuration` from "@/lib/format" and `format` from "date-fns".
    Export:
    - type `InvoiceLineItem = { date: string; durationMinutes: number; amountCents: number; notes: string | null }`.
    - `buildLineItems(sessions)`: map session rows → `InvoiceLineItem[]` (pass through date
      string, durationMinutes, amountCents, notes).
    - `formatPeriod(startDate, endDate)`: both are "yyyy-MM-dd" strings. If equal, return a
      single `format(new Date(\`${startDate}T00:00:00\`), "PPP")`. Else return
      `\`${format(start,"PPP")} – ${format(end,"PPP")}\`` using an en dash with spaces both
      sides (D-03, UI-SPEC period label).
    - `renderInvoiceText(studentName, period, items)`: build the `{invoice}` block exactly
      per UI-SPEC Data Formatting Conventions — header line `Tutoring Invoice — ${studentName}`
      (em dash, D-05: no branding, D-04: no number), then the period label line, then a blank
      line, then one line PER item `${dateLabel} · ${durationLabel} · ${amountLabel}` (middot
      separators, `formatCents`/`formatDuration` for amount/duration, `format(...,"PPP")` for
      date) followed — ONLY when the item has a non-empty note — by a second line indented two
      spaces `  ${note}` (blank/absent notes omit the second line entirely, no dash
      placeholder, D-01); then a blank line, then `Total: ${formatCents(totalCents)}`.
    - `MERGE_FIELDS = ["invoice","student","total","zelle","period"] as const` and
      `renderTemplate(template, values)`: sequential `replaceAll("{field}", values[field])`
      over MERGE_FIELDS (RESEARCH Pattern 3). Unknown tokens left verbatim — no throw, no
      strip.

    `lib/validation/invoice.ts`: minimal — invoice content is never user-typed. Export
    `invoiceGenerateSchema = z.object({ studentId: z.coerce.number("Invalid student.").int().positive("Invalid student.") })`.
    (Delete uses a bound-arg id guard in Plan 03, not a form schema.)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
    <automated>grep -q 'from "@/lib/format"' lib/invoice/render.ts && grep -q "replaceAll" lib/invoice/render.ts && grep -q "MERGE_FIELDS" lib/invoice/render.ts && echo OK</automated>
    <automated>grep -Eq "Total:" lib/invoice/render.ts && grep -q "Tutoring Invoice" lib/invoice/render.ts && echo TEXT_OK</automated>
  </verify>
  <acceptance_criteria>
    - render.ts imports formatCents/formatDuration from @/lib/format (does NOT redefine money/duration formatting).
    - renderTemplate leaves unknown tokens verbatim (no throw path in source).
    - formatPeriod collapses to a single date when start === end (source branch present).
    - `npx tsc --noEmit` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>Pure, reusable invoice-text + template-merge functions exist; generate input schema defined.</done>
</task>

<task type="auto">
  <name>Task 2: generateInvoiceAction (atomic) + preview modal + Dashboard trigger</name>
  <files>lib/actions/invoices.ts, components/invoice-preview-dialog.tsx, components/dashboard-table.tsx</files>
  <read_first>
    - lib/actions/sessions.ts (addSessionAction re-fetch-server-side precedent; state shape; revalidatePath usage)
    - lib/actions/settings.ts + lib/invoice/defaults.ts (read settings row / fall back to defaults)
    - lib/invoice/render.ts (Task 1 exports)
    - lib/validation/invoice.ts (Task 1)
    - components/session-form-dialog.tsx (Dialog shell + useActionState + "close only on real success" adjust-during-render pattern; non-authoritative client preview precedent)
    - components/dashboard-table.tsx (the file being modified — current single-button row header; DASHBOARD row props)
    - .planning/phases/03-invoicing-email-history/03-RESEARCH.md (Pitfall 1 resolution 1 two-step; Pitfall 2 NEVER db.transaction; Anti-Patterns: re-SELECT server-side)
    - .planning/phases/03-invoicing-email-history/03-UI-SPEC.md (Surface 2 DOM restructure; Surface 3 preview modal; Copywriting Contract generate rows)
  </read_first>
  <action>
    `lib/actions/invoices.ts` ("use server"): export interface `InvoiceActionState`
    `{ fieldErrors: Record<string,string[]> | null; invoiceId: number | null }`. Export
    `generateInvoiceAction(_prev, formData)`:
    1. `invoiceGenerateSchema.safeParse({ studentId: formData.get("studentId") })`; on failure
       return `{ fieldErrors, invoiceId: null }`.
    2. Re-SELECT server-side (NEVER trust client-echoed ids/total — Anti-Patterns / D-08):
       `db.select().from(sessions).where(and(eq(sessions.studentId, id), eq(sessions.billed, false))).orderBy(asc(sessions.date))`.
       If empty → return `{ fieldErrors: { studentId: ["This student has no unbilled sessions to invoice."] }, invoiceId: null }`
       (UI-SPEC defensive error / race guard).
    3. Fetch the student (name, parentEmail) and the settings row (fall back to
       DEFAULT_SUBJECT_TEMPLATE/DEFAULT_BODY_TEMPLATE + "" zelle when no row).
    4. Compute `totalCents` = sum of the sessions' stored `amountCents` (P2 D-14 — NOT
       re-derived from the current rate). `periodStart` = first (min) date, `periodEnd` =
       last (max) date. `lineItems = buildLineItems(rows)`. `invoiceBlock =
       renderInvoiceText(studentName, formatPeriod(periodStart, periodEnd), lineItems)`.
       `values = { invoice: invoiceBlock, student: studentName, total: formatCents(totalCents),
       zelle: settings.zelleHandle, period: formatPeriod(periodStart, periodEnd) }`.
       `renderedBody = renderTemplate(bodyTemplate, values)`; `renderedSubject =
       renderTemplate(subjectTemplate, values)` (Pitfall 4: freeze the FULLY rendered body +
       subject, never re-render later).
    5. Atomicity via Pitfall 1 resolution 1 (single-user app): first a lone
       `const [row] = await db.insert(invoices).values({ studentId, periodStart, periodEnd,
       totalCents, lineItems, renderedBody, renderedSubject }).returning({ id: invoices.id })`,
       THEN `await db.batch([ db.update(sessions).set({ billed: true, invoiceId: row.id })
       .where(inArray(sessions.id, rows.map(r => r.id))) ])`. Use `db.batch`, NEVER
       `db.transaction` (Pitfall 2 — throws at runtime on neon-http, build won't catch it).
    6. `revalidatePath("/dashboard")`, `revalidatePath("/history")`; return
       `{ fieldErrors: null, invoiceId: row.id }`.

    `components/invoice-preview-dialog.tsx` ("use client"): clone session-form-dialog's
    Dialog shell + `useActionState(generateInvoiceAction, { fieldErrors: null, invoiceId: null })`.
    Props: the student (id + name), that student's unbilled `sessions`, and the current
    `settings` (zelleHandle/subjectTemplate/bodyTemplate) — all passed down from the Dashboard
    Server Component. Title "Generate Invoice for {student}"; DialogDescription = the
    irreversibility warning (Copywriting Contract). Body = a CLIENT-SIDE preview only
    (non-authoritative, mirrors the Log Session "≈ $X.XX" precedent): compute the same
    renderInvoiceText/renderTemplate from the props and render Subject as a labeled
    `text-sm font-semibold` line ("Subject:" prefix `text-zinc-600`) then the body in the
    frozen-text block treatment `rounded-lg border border-zinc-200 bg-zinc-50 p-4 font-mono
    text-sm whitespace-pre-wrap leading-relaxed text-zinc-900`. Hidden `<input name="studentId"
    value={student.id}>`; the form submits ONLY the studentId — the Server Action re-fetches
    everything else. Footer: "Cancel" (DialogClose, outline) + "Generate & Freeze" (accent
    `bg-blue-600 text-white hover:bg-blue-700`, pending "Generating…"). On success (new state
    with `fieldErrors === null` and a non-null `invoiceId`) use the adjust-during-render idiom
    to `setOpen(false)` AND `router.push(\`/history/${state.invoiceId}\`)` (D-09 — navigate to
    the finished invoice, not back to the Dashboard). Import `useRouter` from
    "next/navigation".

    `components/dashboard-table.tsx` (MODIFY — Surface 2 DOM restructure): a `<button>` cannot
    nest a `<button>`, so replace the outer full-row `<button>` with an outer
    `<div className="flex min-h-11 w-full items-center gap-2 px-4 py-2">` containing (1) an
    inner `<button type="button" className="flex flex-1 items-center gap-2 text-left"
    onClick={toggle}>` holding ONLY the chevron + student name; (2) a plain `<span
    className="flex items-center gap-4">` holding duration + amount (no longer clickable); (3)
    an `<InvoicePreviewDialog>` rendered with `variant="outline" size="sm"` trigger labeled
    "Generate Invoice", passed this row's student + its `sessionsByStudentId[row.id]` +
    the settings — but ONLY when `row.unbilledAmountCents !== 0` (D-08: hidden, not disabled,
    at $0). Preserve the existing expand/collapse body untouched. The DashboardTable +
    DashboardRowItem now also need a `settings` prop threaded from the page.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
    <automated>grep -q "db.batch" lib/actions/invoices.ts && ! grep -q "db.transaction" lib/actions/invoices.ts && echo BATCH_OK</automated>
    <automated>grep -q "billed: false" lib/actions/invoices.ts && grep -q "returning" lib/actions/invoices.ts && grep -q "InvoicePreviewDialog" components/dashboard-table.tsx && echo WIRED</automated>
  </verify>
  <acceptance_criteria>
    - generateInvoiceAction uses `db.batch` and contains NO `db.transaction` reference (source assertion — Pitfall 2).
    - Action re-SELECTs unbilled sessions server-side (does not read a session-id list or total from FormData).
    - totalCents summed from stored `amountCents`, not re-derived from student rate.
    - renderedBody/renderedSubject are frozen into the insert (Pitfall 4).
    - Dashboard row no longer nests a button in a button; Generate trigger omitted when unbilledAmountCents === 0.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Generating from the Dashboard atomically freezes a snapshot + bills sessions and routes to the invoice.</done>
</task>

<task type="auto">
  <name>Task 3: Shared invoice view (frozen text + Copy) + /history/[id] route</name>
  <files>components/invoice-view.tsx, app/(app)/history/[id]/page.tsx</files>
  <read_first>
    - lib/invoice/render.ts (formatPeriod for the metadata line)
    - lib/format.ts (formatCents for the total)
    - components/student-table.tsx (renderActions flex gap-2 action-row grouping; page/card conventions)
    - app/(app)/dashboard/page.tsx (Server Component read + container classes + notFound-style handling)
    - lib/db/schema.ts (invoices columns to select)
    - .planning/phases/03-invoicing-email-history/03-UI-SPEC.md (Surface 4: heading, metadata line, frozen block treatment, Copy behavior + fallback textarea; Copywriting Contract copy rows)
    - .planning/phases/03-invoicing-email-history/03-RESEARCH.md (Pitfall 4: render from FROZEN columns, never live sessions; Clipboard Code Example)
  </read_first>
  <action>
    `components/invoice-view.tsx` ("use client"): the shared frozen-snapshot view (reused by
    History open in Plan 04 and extended with Email/Delete in Plan 03). Props (wire ALL now so
    Plan 03 edits only this file, not the page): `invoiceId`, `studentName`, `parentEmail`,
    `periodStart`, `periodEnd`, `totalCents`, `generatedAt` (Date), `renderedSubject`,
    `renderedBody`, `sessionCount`. Render:
    - `<h1 className="text-[28px] leading-tight font-semibold text-zinc-900">Invoice — {studentName}</h1>`.
    - metadata line `text-sm text-zinc-600`: `{formatPeriod(periodStart,periodEnd)} · Generated
      {format(generatedAt,"PPP")} · {formatCents(totalCents)}`.
    - A labeled `Subject: {renderedSubject}` line, then the frozen BODY in the block treatment
      `rounded-lg border border-zinc-200 bg-zinc-50 p-4 font-mono text-sm whitespace-pre-wrap
      leading-relaxed text-zinc-900`. Render `renderedBody` as a plain React text node inside a
      `<pre>`/`whitespace-pre-wrap` container — NEVER `dangerouslySetInnerHTML` (T-03-03 —
      notes are parent-facing, template is free text).
    - Action row `<div className="flex gap-2">`: for THIS plan include only the "Copy Invoice
      Text" button (outline) — Email + Delete are added in Plan 03 (leave a clear
      `{/* Email Invoice + Delete Invoice added in Plan 03 */}` comment so the executor of Plan
      03 knows where they slot). Copy button copies the frozen `renderedBody` (body only, not
      subject) via `await navigator.clipboard.writeText(renderedBody)`; on success swap the
      label to "Copied!" for ~2s (local useState + setTimeout) then revert; on throw, reveal a
      read-only auto-selected `<textarea>` below the buttons prefilled with renderedBody +
      caption "Select the text above and press Cmd/Ctrl+C to copy." (MAIL-03, D-11).

    `app/(app)/history/[id]/page.tsx` (Server Component): params `{ id }` (await params per
    Next 16). Parse the id to a positive integer; if invalid → `notFound()`. Read the invoice
    joined to its student:
    `db.select({ invoice: invoices, studentName: students.name, parentEmail: students.parentEmail })
    .from(invoices).leftJoin(students, eq(invoices.studentId, students.id)).where(eq(invoices.id, id))`.
    If no row → `notFound()`. Compute `sessionCount` from `(invoice.lineItems as InvoiceLineItem[]).length`.
    Render the page container (`mx-auto w-full max-w-3xl px-4 py-8 sm:py-12`) and pass every
    field into `<InvoiceView .../>`. This view renders ENTIRELY from the frozen invoice columns
    — it never queries `sessions` (Pitfall 4 / INV-04).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run build</automated>
    <automated>grep -q "renderedBody" components/invoice-view.tsx && ! grep -q "dangerouslySetInnerHTML" components/invoice-view.tsx && grep -q "clipboard.writeText" components/invoice-view.tsx && echo VIEW_OK</automated>
    <automated>grep -q "from(invoices)" 'app/(app)/history/[id]/page.tsx' && ! grep -q "from(sessions)" 'app/(app)/history/[id]/page.tsx' && grep -q "notFound" 'app/(app)/history/[id]/page.tsx' && echo ROUTE_OK</automated>
    <human-check>Generate an invoice for a student with several unbilled sessions (include one with a note and one without). Confirm: you land on /history/[id]; the frozen text shows the "Tutoring Invoice — {name}" header, the period range, itemized lines with the noted session showing its indented note and the un-noted one omitting it, and the correct Total; Copy Invoice Text puts the body on the clipboard and shows "Copied!". Then edit that billed session's hours on the Sessions page and reload the invoice — the frozen total/lines must be UNCHANGED (INV-04).</human-check>
  </verify>
  <acceptance_criteria>
    - invoice-view renders renderedBody via a text node / whitespace-pre-wrap — no `dangerouslySetInnerHTML` (source assertion).
    - /history/[id] page selects from `invoices` and never from `sessions` (source assertion — INV-04).
    - Invalid or missing id → `notFound()`.
    - Copy button uses `navigator.clipboard.writeText` with a textarea fallback path.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Post-generate landing shows the immutable snapshot; a billed-session edit does not alter it; copy works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| preview form → generateInvoiceAction | Only studentId crosses; all invoice data is re-fetched server-side |
| billed sessions ↔ frozen invoice | Snapshot must be copied data, never a live join |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | generateInvoiceAction total/session IDs | mitigate | Re-SELECT unbilled sessions server-side; totalCents summed from stored amountCents; client sends only studentId (RESEARCH Anti-Patterns) |
| T-03-05 | Repudiation/Integrity | atomic bill + freeze | mitigate | `db.batch` (real BEGIN/COMMIT/rollback) for the sessions update; single INSERT…RETURNING first (Pitfall 1 res.1); NEVER db.transaction (Pitfall 2) |
| T-03-06 | Tampering (immutability) | invoice snapshot vs live sessions | mitigate | Freeze lineItems + renderedBody + renderedSubject + totalCents; view reads only frozen columns, never re-joins sessions (INV-04, Pitfall 4) |
| T-03-07 | Tampering (Stored XSS) | renderedBody / notes on invoice view | mitigate | Plain React text node / whitespace-pre-wrap; no `dangerouslySetInnerHTML` (D-02 notes now parent-facing) |
| T-03-08 | Information Disclosure | /history/[id] | accept | Global Phase 1 middleware gate auto-covers /history/*; no matcher change |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` pass.
- Source assertions: `db.batch` present, `db.transaction` absent, no `dangerouslySetInnerHTML`, invoice view/route read only frozen columns.
- End-of-phase human-check: full generate → freeze → land → copy flow, plus the INV-04 immutability check (edit a billed session, invoice text unchanged).
</verification>

<success_criteria>
- INV-01: invoice totals ALL of a student's unbilled sessions.
- INV-02: produces a copyable text summary.
- INV-03: generation atomically bills sessions + stores an immutable snapshot.
- INV-04: later session edits/deletes never alter a generated invoice.
- MAIL-03: Copy Invoice Text fallback works (with textarea fallback on failure).
</success_criteria>

<output>
Create `.planning/phases/03-invoicing-email-history/03-02-SUMMARY.md` when done.
</output>
