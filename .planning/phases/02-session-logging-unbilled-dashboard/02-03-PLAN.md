---
phase: 02-session-logging-unbilled-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - components/dashboard-table.tsx
  - app/(app)/dashboard/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02]
must_haves:
  truths:
    - "The Dashboard shows every ACTIVE student with their total unbilled hours and amount owed"
    - "Students are sorted most-owed first; $0 students appear at the bottom (shown, not hidden)"
    - "Billed sessions are excluded from the unbilled hours/amount totals"
    - "Expanding a student reveals their underlying unbilled sessions with a path to edit one"
    - "Archived students never appear on the Dashboard"
  artifacts:
    - path: "components/dashboard-table.tsx"
      provides: "per-student unbilled rows (expandable), responsive, edit path"
      contains: "DashboardTable"
    - path: "app/(app)/dashboard/page.tsx"
      provides: "Dashboard route: LEFT JOIN + GROUP BY + FILTER aggregate query + rendering"
      contains: "leftJoin"
  key_links:
    - from: "app/(app)/dashboard/page.tsx"
      to: "sessions aggregate (sum filter billed=false)"
      via: "sql filter aggregate over leftJoin"
      pattern: "filter \\(where"
    - from: "components/dashboard-table.tsx"
      to: "SessionFormDialog (edit mode)"
      via: "expanded-row Edit action"
      pattern: "SessionFormDialog"
---

<objective>
Deliver the unbilled dashboard: a glance-able, money-aware roster showing every active student's total unbilled hours and amount owed, sorted most-owed first, with $0 students falling to the bottom, and expandable to the underlying unbilled sessions with an edit path (D-11, D-12, D-13 / DASH-01, DASH-02).

The core correctness concern is the aggregate query: a `LEFT JOIN` + `GROUP BY` + `FILTER (WHERE billed = false)` so that students with zero sessions (or only billed sessions) still appear at $0, while billed sessions are excluded from the sums.

Purpose: Answer "who do I invoice next?" at a glance while keeping the full active roster visible.
Output: the dashboard aggregate query + `DashboardTable` component + populated Dashboard page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-logging-unbilled-dashboard/02-CONTEXT.md
@.planning/phases/02-session-logging-unbilled-dashboard/02-RESEARCH.md
@.planning/phases/02-session-logging-unbilled-dashboard/02-PATTERNS.md
@.planning/phases/02-session-logging-unbilled-dashboard/02-UI-SPEC.md
@.planning/phases/02-session-logging-unbilled-dashboard/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
New symbols/files introduced by THIS plan (exclude from external-drift verification):
- `DashboardTable` in `components/dashboard-table.tsx`
- Populated route `app/(app)/dashboard/page.tsx` (aggregate query + rendering)
(Consumes from earlier plans: `sessions`/`students` tables, `formatCents`, `formatDuration`, `SessionFormDialog` edit mode, `components/ui/table`.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Unbilled aggregate query + Dashboard page shell</name>
  <files>app/(app)/dashboard/page.tsx</files>
  <read_first>
    - app/(app)/dashboard/page.tsx (placeholder shell from 02-01)
    - app/(app)/page.tsx (Server Component db.select + page-shell/heading convention)
    - lib/db/schema.ts (students + sessions columns)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-RESEARCH.md (Pattern 6 aggregate query; Pitfall 4 LEFT JOIN + FILTER, NOT inner join / WHERE billed; the MEDIUM-confidence note on reusing an sql expr in orderBy — verify TS inference at write time)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-PATTERNS.md (app/(app)/dashboard/page.tsx section)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-UI-SPEC.md (Surface 4 Dashboard: sort order, $0 de-emphasis, info note)
  </read_first>
  <action>
    Fill `app/(app)/dashboard/page.tsx` (Server Component) with the RESEARCH Pattern 6 aggregate. Build two reusable sql expressions: `unbilledMinutesExpr = sql<number>\`coalesce(sum(${sessions.durationMinutes}) filter (where ${sessions.billed} = false), 0)\`` and `unbilledAmountExpr = sql<number>\`coalesce(sum(${sessions.amountCents}) filter (where ${sessions.billed} = false), 0)\``. Query `db.select({ id, name, unbilledMinutes: unbilledMinutesExpr.mapWith(Number), unbilledAmountCents: unbilledAmountExpr.mapWith(Number) }).from(students).leftJoin(sessions, eq(sessions.studentId, students.id)).where(eq(students.archived, false)).groupBy(students.id, students.name).orderBy(desc(unbilledAmountExpr), asc(students.name))`. Use `LEFT JOIN` (so $0 / no-session students still appear — D-12) and the aggregate `FILTER (WHERE billed = false)` (so billed sessions are excluded from the sum while the student still shows — DASH-02, Pitfall 4); do NOT add a `WHERE sessions.billed = false` clause. Verify the Drizzle TS inference for reusing the same `sql` expression in both select and orderBy compiles cleanly (RESEARCH flags this as version-nuanced) — if the ordering alias errors, order by the same sql template re-expressed rather than switching to an inner join or a JS re-sort.

    Also fetch the active student list (for the expanded-row Edit path's combobox in Task 2). Render the page shell matching app/(app)/page.tsx: `<div className="mx-auto w-full max-w-3xl px-4 py-8 sm:py-12">` + `<h1 className="text-[28px] leading-tight font-semibold text-zinc-900">Dashboard</h1>` with NO primary CTA (glance view, not an add entry point). When zero sessions exist app-wide, render the de-emphasized info note "No sessions logged yet — amounts will appear here once you log your first session." (`text-sm text-zinc-600` under the h1) — the full roster still renders below at $0.00 / 0 hrs (not a blocking empty state). Pass the aggregate rows + student list into `DashboardTable` (Task 2).
  </action>
  <verify>
    <automated>grep -q "leftJoin" "app/(app)/dashboard/page.tsx" && grep -q "filter (where" "app/(app)/dashboard/page.tsx" && grep -q "eq(students.archived, false)" "app/(app)/dashboard/page.tsx" && grep -q "desc(" "app/(app)/dashboard/page.tsx" && ! grep -Eq "innerJoin\\(sessions|where\\(eq\\(sessions.billed" "app/(app)/dashboard/page.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The query uses `leftJoin` on sessions and an aggregate `filter (where ... billed = false)` — NOT an inner join and NOT a `WHERE sessions.billed = false` clause (Pitfall 4)
    - `where(eq(students.archived, false))` excludes archived students (D-12)
    - Ordering is `desc(unbilledAmountExpr)` then `asc(students.name)` (most-owed first, alpha tiebreak)
    - `coalesce(..., 0)` yields 0 for students with no matching sessions
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The Dashboard page runs the correct unbilled aggregate query (all active students, billed excluded from sums, most-owed-first) and passes rows to the table. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: DashboardTable — per-student unbilled rows, expandable to unbilled sessions with edit</name>
  <files>components/dashboard-table.tsx</files>
  <read_first>
    - components/student-table.tsx (responsive table md+/card mobile split, empty-state shape)
    - components/session-table.tsx (02-02 — the collapsible disclosure + 44px header pattern to mirror; do NOT edit it, replicate the affordance)
    - components/session-form-dialog.tsx (02-02 — SessionFormDialog edit mode for the expanded-row Edit path)
    - lib/format.ts (formatCents, formatDuration)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-PATTERNS.md (dashboard-table.tsx section)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-UI-SPEC.md (Surface 4: collapsed row content, $0 de-emphasis, expanded = unbilled sessions with Edit only)
  </read_first>
  <action>
    Create `components/dashboard-table.tsx` (`"use client"` for the disclosure toggle). Props: the aggregate rows `{ id, name, unbilledMinutes, unbilledAmountCents }[]`, the per-student unbilled sessions (for expansion), and the active student list (for the edit dialog). Reuse student-table.tsx's responsive table→card split and mirror session-table.tsx's collapsible header affordance (chevron, `min-h-11` 44px tap target, collapsed by default per D-11). Collapsed row/card content: student name + unbilled hours via `formatDuration(unbilledMinutes)` + amount owed via `formatCents(unbilledAmountCents)` side by side. A $0 / 0-hour student still renders (never hidden — D-12) but de-emphasized with `text-zinc-600` instead of `text-zinc-900`, so paying attention flows to debtors first. Expanded content: that student's UNBILLED sessions only (D-13 — never billed rows; the page already excludes them from the sum, and the expansion list must likewise show only unbilled), same responsive table→card body as the Sessions page but with a single Edit action per row (`SessionFormDialog` mode="edit") — NO Delete here (Delete stays on the Sessions page to avoid two destructive entry points; Edit is included because D-11 explicitly says "with a path to edit"). Rows come pre-sorted from the server query — do not client re-sort.
  </action>
  <verify>
    <automated>grep -q "DashboardTable" components/dashboard-table.tsx && grep -q "formatDuration" components/dashboard-table.tsx && grep -q "formatCents" components/dashboard-table.tsx && grep -q "SessionFormDialog" components/dashboard-table.tsx && grep -q "min-h-11" components/dashboard-table.tsx && grep -q "text-zinc-600" components/dashboard-table.tsx && ! grep -q "SessionDeleteConfirmDialog" components/dashboard-table.tsx && npm run build</automated>
    <human-check>On /dashboard: all active students show (debtors first, $0 students de-emphasized at the bottom); expanding a student reveals only their unbilled sessions with an Edit action; archived students are absent. Log/bill nothing changes the sort live after a session edit.</human-check>
  </verify>
  <acceptance_criteria>
    - Every active student renders (including $0), sorted as the server provided (no client re-sort); $0 students use `text-zinc-600` de-emphasis
    - Collapsed content shows `formatDuration` hours + `formatCents` amount owed; header is a 44px (`min-h-11`) tap target with a chevron, collapsed by default
    - Expanded content lists only unbilled sessions with an Edit action (`SessionFormDialog` edit); no Delete action present
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The Dashboard renders every active student with correct unbilled totals, most-owed-first, $0 de-emphasized, expandable to unbilled sessions with an edit path. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → aggregate sql template | Computed sums rendered as money must be parameterized, not string-concatenated |
| authenticated browser → /dashboard | Read-only aggregate behind the auth gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | `sql` aggregate template in the dashboard query | mitigate | Drizzle `` sql`...` `` interpolates only schema column references (`${sessions.durationMinutes}`, `${sessions.billed}`), which are parameterized/identifier-safe — never raw user input. No SQL injection surface (Security Domain, ASVS V5). |
| T-02-09 | Information disclosure | billed sessions leaking into unbilled totals | mitigate | Aggregate `FILTER (WHERE billed = false)` excludes billed rows from both sums; expansion lists only unbilled sessions (D-13/DASH-02). Prevents over-billing a parent for already-invoiced sessions. |
| T-02-08 | Spoofing | /dashboard behind auth | accept | Covered by the existing iron-session `middleware.ts` gate; route-group restructure (02-01) does not change the matcher. Read-only page, single-user model (ASVS V4). |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` exit 0.
- A student with only billed sessions shows $0 (still visible); a student with no sessions shows $0.
- Grep gates confirm LEFT JOIN + FILTER (not inner join / WHERE billed).
</verification>

<success_criteria>
- Dashboard shows all active students' unbilled hours + amount owed at a glance (DASH-01).
- Billed sessions are excluded from totals (DASH-02).
- Most-owed-first ordering, $0 at bottom, expandable to unbilled sessions with edit (D-11/D-12/D-13).
</success_criteria>

<output>
Create `.planning/phases/02-session-logging-unbilled-dashboard/02-03-SUMMARY.md` when done.
</output>
