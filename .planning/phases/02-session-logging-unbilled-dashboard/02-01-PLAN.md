---
phase: 02-session-logging-unbilled-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema.ts
  - lib/format.ts
  - components/student-table.tsx
  - components/ui/combobox.tsx
  - components/ui/select.tsx
  - components/ui/popover.tsx
  - components/ui/calendar.tsx
  - components/top-nav.tsx
  - app/(app)/layout.tsx
  - app/(app)/page.tsx
  - app/(app)/archived/page.tsx
  - app/(app)/dashboard/page.tsx
  - app/(app)/sessions/page.tsx
  - app/page.tsx
  - app/archived/page.tsx
  - package.json
autonomous: true
requirements: [SESS-01, SESS-05]
must_haves:
  truths:
    - "The `sessions` table exists in the Neon database with columns id, student_id, date, duration_minutes, amount_cents, notes, billed, created_at"
    - "A top nav with three destinations (Students / Dashboard / Sessions) renders on every authenticated page"
    - "The nested Students/Archived sub-tab pair still renders under the Students page (Archived is NOT promoted to a top-level nav item)"
    - "Navigating to /dashboard and /sessions loads a page (heading visible) instead of 404"
    - "The root / URL still serves the student roster; /login renders with NO top nav"
  artifacts:
    - path: "lib/db/schema.ts"
      provides: "sessions Drizzle table with FK studentId → students.id (onDelete restrict)"
      contains: "export const sessions"
    - path: "lib/format.ts"
      provides: "formatCents and formatDuration display helpers"
      exports: ["formatCents", "formatDuration"]
    - path: "components/top-nav.tsx"
      provides: "app-level Students/Dashboard/Sessions nav with active-link styling"
      contains: "TopNav"
    - path: "app/(app)/layout.tsx"
      provides: "route-group layout rendering TopNav above children"
      min_lines: 8
    - path: "app/(app)/dashboard/page.tsx"
      provides: "reachable Dashboard page shell (filled by 02-03)"
    - path: "app/(app)/sessions/page.tsx"
      provides: "reachable Sessions page shell (filled by 02-02)"
  key_links:
    - from: "lib/db/schema.ts sessions.studentId"
      to: "students.id"
      via: "references() with onDelete restrict"
      pattern: "references\\(\\(\\) => students.id"
    - from: "app/(app)/layout.tsx"
      to: "components/top-nav.tsx"
      via: "import + render TopNav"
      pattern: "TopNav"
---

<objective>
Establish the shared foundation for Phase 2 and upgrade the app's information architecture: add the `sessions` table to the schema and push it live, install the shadcn UI primitives the session form needs, extract the money/duration formatting helpers, and restructure the authenticated pages into an `app/(app)/` route group with a real top nav (Students / Dashboard / Sessions per D-02).

After this plan the tutor sees the new three-destination nav, can navigate to (empty) Dashboard and Sessions pages, the `/` roster and `/login` behave exactly as before, and the database has a `sessions` table ready for the logging slice.

Purpose: Every later Phase 2 slice consumes the `sessions` table, the shadcn components, the format helpers, and the nav shell — this plan is the shared prerequisite.
Output: `sessions` table (live in Neon), `lib/format.ts`, four new `components/ui/*` primitives, `components/top-nav.tsx`, the `app/(app)/` route group with moved + placeholder pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-session-logging-unbilled-dashboard/02-CONTEXT.md
@.planning/phases/02-session-logging-unbilled-dashboard/02-RESEARCH.md
@.planning/phases/02-session-logging-unbilled-dashboard/02-PATTERNS.md
@.planning/phases/02-session-logging-unbilled-dashboard/02-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
New symbols/files introduced by THIS plan (exclude from external-drift verification):
- Table: `sessions` (columns: `id`, `student_id`, `date`, `duration_minutes`, `amount_cents`, `notes`, `billed`, `created_at`) in `lib/db/schema.ts` → `export const sessions`
- Functions: `formatCents(cents: number): string`, `formatDuration(minutes: number): string` in `lib/format.ts`
- Component: `TopNav` in `components/top-nav.tsx`
- Layout: `app/(app)/layout.tsx` (default export `AppLayout`)
- Routes (moved): `app/(app)/page.tsx`, `app/(app)/archived/page.tsx`
- Routes (new placeholder): `app/(app)/dashboard/page.tsx`, `app/(app)/sessions/page.tsx`
- shadcn primitives: `components/ui/combobox.tsx`, `components/ui/select.tsx`, `components/ui/popover.tsx`, `components/ui/calendar.tsx`
- Dependency: `react-day-picker` (transitive, added by `shadcn add calendar`)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add sessions table, extract format helpers, install shadcn primitives</name>
  <files>lib/db/schema.ts, lib/format.ts, components/student-table.tsx, components/ui/combobox.tsx, components/ui/select.tsx, components/ui/popover.tsx, components/ui/calendar.tsx, package.json</files>
  <read_first>
    - lib/db/schema.ts (current schema — the `students` table + comment-annotation style to mirror)
    - components/student-table.tsx (source of the inline `formatRate` at lines 31-33 to extract)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-PATTERNS.md (schema + lib/format.ts sections — near-verbatim target code)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-RESEARCH.md (Pattern 5 sessions schema; Standard Stack install command)
    - components.json (confirm `style: "base-nova"` so shadcn add pulls Base UI variants)
  </read_first>
  <action>
    Add `export const sessions = pgTable("sessions", {...})` to lib/db/schema.ts with columns: `id` serial primary key; `studentId` integer("student_id").notNull().references(() => students.id, { onDelete: "restrict" }); `date` date("date", { mode: "string" }).notNull(); `durationMinutes` integer("duration_minutes").notNull(); `amountCents` integer("amount_cents").notNull(); `notes` text("notes") (nullable); `billed` boolean("billed").notNull().default(false); `createdAt` timestamp("created_at").notNull().defaultNow(). Add `date` and `text` to the existing `drizzle-orm/pg-core` import line (do not add a second import statement). Annotate each column with an inline `//` comment naming the decision it enforces (D-14 frozen snapshot on amountCents, SESS-02 optional on notes, DASH-02 read-only on billed, "never cascade — archived students keep history" on the FK), mirroring the existing students-table comment style.

    Create lib/format.ts exporting `formatCents(cents: number): string` returning `$${(cents / 100).toFixed(2)}` (identical output to the current `formatRate`) and `formatDuration(minutes: number): string` returning the "{h} hr(s) {m} min" form per UI-SPEC Data Formatting Conventions — omit the zero part ("45 min", "2 hrs", "1 hr", "1 hr 30 min"), pluralize "hr"/"hrs".

    Update components/student-table.tsx to import `formatCents` from `@/lib/format` and replace its local `formatRate` uses with `formatCents` (either rename call sites or keep a `const formatRate = formatCents` alias) — remove the now-duplicated inline definition.

    Install the Base UI shadcn primitives with `npx shadcn@latest add combobox select popover calendar`. Confirm `react-day-picker` lands in package.json (pulled transitively by the calendar component). Do NOT hand-write these components; do NOT copy Radix `asChild` examples — the installed files must use the Base UI `render`-prop idiom.
  </action>
  <verify>
    <automated>grep -q "export const sessions" lib/db/schema.ts && grep -q "onDelete: \"restrict\"" lib/db/schema.ts && grep -Eq "export function formatCents|export const formatCents" lib/format.ts && grep -q "export function formatDuration" lib/format.ts && test -f components/ui/combobox.tsx && test -f components/ui/select.tsx && test -f components/ui/popover.tsx && test -f components/ui/calendar.tsx && grep -q "react-day-picker" package.json && grep -q "@/lib/format" components/student-table.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - lib/db/schema.ts contains `export const sessions` with a `studentId` column referencing `students.id` and `onDelete: "restrict"`
    - lib/db/schema.ts `date` column uses `mode: "string"` and `amountCents`/`durationMinutes` are integer columns
    - lib/format.ts exports `formatCents` and `formatDuration`; `formatCents(7500)` returns `"$75.00"`; `formatDuration(90)` returns `"1 hr 30 min"`; `formatDuration(45)` returns `"45 min"`
    - components/student-table.tsx imports `formatCents` from `@/lib/format` and no longer defines its own money-formatting one-liner
    - components/ui/combobox.tsx, select.tsx, popover.tsx, calendar.tsx exist; package.json lists `react-day-picker`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>The sessions table is defined in schema source, shared format helpers exist and are consumed by student-table, and the four shadcn primitives are installed with react-day-picker present. Typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push schema — create the sessions table in Neon</name>
  <files>(no source files — runs drizzle-kit against the live DB)</files>
  <read_first>
    - drizzle.config.ts (confirms it loads `.env.local` and points at lib/db/schema.ts)
    - .planning/STATE.md (Phase 1 note: `drizzle-kit push` was the established flow; no committed migration files under ./drizzle)
    - lib/db/schema.ts (the schema just modified in Task 1 — must be saved before pushing)
  </read_first>
  <action>
    BLOCKING and mandatory: run `npx drizzle-kit push` to apply the new `sessions` table to the Neon database. This must run AFTER Task 1's schema.ts change and BEFORE any verification — build and typecheck pass without it (Drizzle types come from schema.ts source, not the live DB), which would be a false-positive "green" state where the app compiles but the `sessions` table does not exist. Follow Phase 1's established flow (`push` directly — there are no committed migration files under ./drizzle). Adding a brand-new table is additive and non-interactive (no rename/data-loss prompts). If `drizzle-kit push` unexpectedly prompts and cannot proceed non-interactively in this environment, stop and surface it for manual execution rather than guessing an answer.
  </action>
  <verify>
    <automated>node -e "require('dotenv').config({path:'.env.local'}); const { neon } = require('@neondatabase/serverless'); const sql = neon(process.env.DATABASE_URL); sql\`select to_regclass('public.sessions') as t\`.then(r => { if (!r[0].t) { console.error('sessions table missing'); process.exit(1); } console.log('sessions table present'); });"</automated>
    <human-check>Confirm in the Neon console (or `npx drizzle-kit studio`) that the `sessions` table has columns student_id, date, duration_minutes, amount_cents, notes, billed, created_at.</human-check>
  </verify>
  <acceptance_criteria>
    - `to_regclass('public.sessions')` returns non-null (table exists in Neon)
    - The live table has a foreign key on `student_id` referencing `students(id)`
    - No migration was applied that dropped or altered the existing `students` or `login_attempts` tables
  </acceptance_criteria>
  <done>The `sessions` table physically exists in the Neon database and the app's runtime queries against it would succeed.</done>
</task>

<task type="auto">
  <name>Task 3: Route-group restructure + top nav (D-01, D-02)</name>
  <files>components/top-nav.tsx, app/(app)/layout.tsx, app/(app)/page.tsx, app/(app)/archived/page.tsx, app/(app)/dashboard/page.tsx, app/(app)/sessions/page.tsx, app/page.tsx, app/archived/page.tsx</files>
  <read_first>
    - app/page.tsx (current roster page + inlined Students/Archived tab strip to move)
    - app/archived/page.tsx (current archived page + mirrored tab strip)
    - app/layout.tsx (root layout — MUST stay nav-free; fonts/html/body only)
    - components/ui/dialog.tsx (this repo's Base UI `render`-prop idiom, for reference)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-PATTERNS.md (top-nav.tsx and app/(app)/layout.tsx sections)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-RESEARCH.md (Pattern 1 route group; Pitfall 6 nav must not leak onto /login)
    - .planning/phases/02-session-logging-unbilled-dashboard/02-UI-SPEC.md (Surface 1 app-level top nav)
  </read_first>
  <action>
    Create the `app/(app)/` route group so the new nav wraps every authenticated page but never `/login` (Pitfall 6). Route groups do not change URLs. Move `app/page.tsx` → `app/(app)/page.tsx` and `app/archived/page.tsx` → `app/(app)/archived/page.tsx` verbatim (URLs stay `/` and `/archived`; keep their inlined nested Students/Archived tab pair unchanged per D-02 — do NOT flatten Archived into the top nav). Delete the old `app/page.tsx` and `app/archived/page.tsx` after moving. Do NOT touch root `app/layout.tsx`.

    Create `components/top-nav.tsx` as a `"use client"` component using `usePathname()` from `next/navigation` to compute the active link. Render three `next/link` items in order — Students (`/`), Dashboard (`/dashboard`), Sessions (`/sessions`) — reusing the existing tab styling: container `mx-auto w-full max-w-3xl px-4` + `flex gap-6 border-b border-zinc-200`, each link `pb-2 text-base font-medium`; active = `text-blue-600 border-b-2 border-blue-600 -mb-px`, inactive = `border-b-2 border-transparent text-zinc-600 hover:text-zinc-900`. Treat `/archived` as still under the Students nav item for active-state purposes (Students active when pathname is `/` or `/archived`).

    Create `app/(app)/layout.tsx` (default export `AppLayout`) that renders `<TopNav />` then `<main>{children}</main>` — no `<html>`/`<body>` (those stay solely in root layout).

    Create placeholder `app/(app)/dashboard/page.tsx` and `app/(app)/sessions/page.tsx` as Server Components using the same page shell as app/(app)/page.tsx (`<div className="mx-auto w-full max-w-3xl px-4 py-8 sm:py-12">` + `<h1 className="text-[28px] leading-tight font-semibold text-zinc-900">`) with headings "Dashboard" and "Sessions" respectively — these are filled with real content by 02-03 and 02-02.
  </action>
  <verify>
    <automated>test -f "app/(app)/layout.tsx" && test -f "app/(app)/page.tsx" && test -f "app/(app)/archived/page.tsx" && test -f "app/(app)/dashboard/page.tsx" && test -f "app/(app)/sessions/page.tsx" && test ! -f app/page.tsx && test ! -f app/archived/page.tsx && grep -q "TopNav" "app/(app)/layout.tsx" && grep -q "usePathname" components/top-nav.tsx && ! grep -q "TopNav" app/layout.tsx && npm run build</automated>
    <human-check>Load the deployed/dev app: `/` shows the roster with the new top nav + nested Students/Archived sub-tabs; `/dashboard` and `/sessions` show their headings; `/login` shows NO top nav.</human-check>
  </verify>
  <acceptance_criteria>
    - `app/page.tsx` and `app/archived/page.tsx` no longer exist; equivalents live under `app/(app)/`
    - `app/(app)/layout.tsx` imports and renders `TopNav`; root `app/layout.tsx` contains no `TopNav` reference (Pitfall 6)
    - `components/top-nav.tsx` uses `usePathname()` and renders exactly three top-level links (Students, Dashboard, Sessions) — Archived is NOT a fourth top-level link
    - `/` and `/archived` still resolve (route group does not change URLs); `/dashboard` and `/sessions` resolve to pages with the correct headings
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>The authenticated pages live in the `app/(app)/` group under a shared top nav; roster stays at `/`, Dashboard and Sessions are reachable shells, and `/login` remains nav-free. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated browser → app routes | Every route except /login must require a valid iron-session cookie |
| local dev machine → Neon DB (drizzle-kit push) | Schema mutation crosses into the production database |
| npm/shadcn registry → repo | New component code + react-day-picker transitive dependency enters the tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Spoofing / Elevation | New /dashboard and /sessions routes | mitigate | Route-group restructure is a build-time file organization concept invisible to the request path; the existing `middleware.ts` matcher already denies-by-default every route except /login, so it continues to gate the new routes. Verify /login renders nav-free and new routes are unreachable without a session. |
| T-02-06 | Tampering / Data loss | drizzle-kit push against Neon | accept | Additive-only table creation (no renames/drops); push is non-interactive for a brand-new table. Verify no existing table was altered. Low risk at single-user scale. |
| T-02-SC | Tampering (supply chain) | `shadcn add` + `react-day-picker` | mitigate | Package Legitimacy Audit in 02-RESEARCH.md verified `react-day-picker` (slopcheck OK, ~11-yr-old, official gpbl repo) and `@base-ui/react` (pre-existing). shadcn official registry, no third-party blocks. No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — no blocking legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run build` both exit 0.
- `sessions` table exists in Neon (`to_regclass` non-null).
- Top nav renders on authenticated pages, absent on /login.
</verification>

<success_criteria>
- The `sessions` table is live in Neon with the specified columns and FK.
- `lib/format.ts` exports `formatCents` + `formatDuration`, consumed by student-table.
- Four shadcn Base UI primitives installed; react-day-picker present.
- Authenticated pages live under `app/(app)/` with a Students/Dashboard/Sessions top nav; `/` still serves the roster; `/login` has no nav.
</success_criteria>

<output>
Create `.planning/phases/02-session-logging-unbilled-dashboard/02-01-SUMMARY.md` when done.
</output>
