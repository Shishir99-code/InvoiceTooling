---
phase: 01-foundation-auth-gate-student-roster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - postcss.config.mjs
  - components.json
  - app/layout.tsx
  - app/globals.css
  - app/page.tsx
  - components/ui/dialog.tsx
  - components/ui/button.tsx
  - components/ui/input.tsx
  - components/ui/label.tsx
  - components/ui/table.tsx
  - lib/utils.ts
  - lib/db/index.ts
  - lib/db/schema.ts
  - lib/session.ts
  - drizzle.config.ts
  - .env.local
  - .env.example
autonomous: false
requirements: [STUD-01, STUD-02, STUD-03, STUD-04, AUTH-04]
user_setup:
  - service: neon
    why: "Persistent Postgres database for students + login_attempts tables (STUD-*, AUTH-04)"
    env_vars:
      - name: DATABASE_URL
        source: "Vercel Dashboard -> Storage -> Neon integration -> connection string (or Neon console -> project -> connection string)"
      - name: APP_PASSWORD
        source: "Chosen by the tutor — a single high-entropy shared password (min 16 chars recommended)"
      - name: SESSION_SECRET
        source: "Generated 32+ char random string for iron-session cookie encryption (e.g. openssl rand -base64 32)"

must_haves:
  truths:
    - "The Next.js dev server starts with npm run dev and compiles with zero type errors"
    - "drizzle-kit push applies the schema to the live Neon database"
    - "The students and login_attempts tables exist in Neon after push"
  artifacts:
    - path: "lib/db/schema.ts"
      provides: "students table (integer-cents rate, required parent email, archived flag) + login_attempts table"
      contains: "export const students = pgTable"
    - path: "lib/db/index.ts"
      provides: "Drizzle db instance over the neon-http driver"
      contains: "drizzle"
    - path: "lib/session.ts"
      provides: "SessionData type, sessionOptions, getSession() helper for iron-session"
      contains: "sessionOptions"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config pointing at lib/db/schema.ts with dotenv-loaded DATABASE_URL"
      contains: "defineConfig"
  key_links:
    - from: "drizzle.config.ts"
      to: "lib/db/schema.ts"
      via: "schema path reference"
      pattern: "schema.*lib/db/schema"
    - from: "lib/db/index.ts"
      to: "process.env.DATABASE_URL"
      via: "neon() driver init"
      pattern: "neon\\(process.env.DATABASE_URL"
---

<objective>
Scaffold the greenfield TutorInvoice app (Next.js 16 App Router + locked stack), wire Drizzle to Neon Postgres, define the `students` and `login_attempts` schema, configure the iron-session helper, and push the schema to the live Neon database. This is the Walking Skeleton base — every later slice (auth gate, roster CRUD, archive) builds on the files and DB tables created here.

Purpose: Establish the foundational patterns (Server-Action-ready structure, Drizzle+Neon, integer-cents money at `rate_cents`, soft-delete `archived` flag) that Phases 1–3 all inherit. Schema choices here have downstream impact and are hard to change later.
Output: A compiling Next.js project with shadcn/ui installed, a live Neon DB containing the `students` and `login_attempts` tables, and the session/db/config plumbing ready for the auth gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
@.planning/phases/01-foundation-auth-gate-student-roster/01-CONTEXT.md
@.planning/phases/01-foundation-auth-gate-student-roster/01-RESEARCH.md
@.planning/phases/01-foundation-auth-gate-student-roster/01-SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js project + install locked stack + shadcn/ui</name>
  <files>package.json, tsconfig.json, next.config.ts, eslint.config.mjs, postcss.config.mjs, components.json, app/layout.tsx, app/globals.css, app/page.tsx, components/ui/dialog.tsx, components/ui/button.tsx, components/ui/input.tsx, components/ui/label.tsx, components/ui/table.tsx, lib/utils.ts</files>
  <read_first>
    - ./CLAUDE.md (Recommended Stack table, Installation, Version Compatibility — exact locked versions)
    - .planning/phases/01-foundation-auth-gate-student-roster/01-RESEARCH.md (Standard Stack table, "Installation" block, Recommended Project Structure)
    - .planning/phases/01-foundation-auth-gate-student-roster/01-UI-SPEC.md (Design System — shadcn preset new-york / neutral, components needed: dialog button input label table)
  </read_first>
  <action>
    Scaffold into the current directory (which already contains CLAUDE.md and .planning/ — the scaffolder must not clobber them) with `npx create-next-app@latest . --typescript --tailwind --eslint --app --turbopack --import-alias "@/*"`. Then install the exact locked runtime deps: `drizzle-orm@0.45.2`, `@neondatabase/serverless@1.1.0`, `zod@4.4.3`, `iron-session@8.0.4`, `date-fns@4.4.0`, `drizzle-zod@0.8.3`, `@vercel/functions@3.7.5`, `dotenv`; and dev dep `drizzle-kit@0.31.10`. Do NOT hand-pick mismatched versions — keep drizzle-orm and drizzle-kit in lockstep. Initialize shadcn with `npx shadcn@latest init` using style `new-york`, base color `neutral`, CSS variables enabled (per UI-SPEC). Add components with `npx shadcn@latest add dialog button input label table`. Confirm Tailwind v4 setup (globals.css uses `@import "tailwindcss";`, no legacy tailwind.config.js). Leave the default `app/page.tsx` in place for now (Plan 02 replaces it with the protected roster). Node ≥ 20.9 is required (local Node v25.1.0 satisfies).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f components/ui/dialog.tsx && test -f components/ui/table.tsx && grep -q '"next": "16' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `next` 16.2.x, `react`/`react-dom` 19.2.x, `drizzle-orm` 0.45.2, `drizzle-kit` 0.31.10, `@neondatabase/serverless` 1.1.0, `zod` 4.4.3, `iron-session` 8.0.4, `drizzle-zod` 0.8.3, `@vercel/functions` 3.7.5
    - Files exist: components/ui/dialog.tsx, components/ui/button.tsx, components/ui/input.tsx, components/ui/label.tsx, components/ui/table.tsx, lib/utils.ts, components.json
    - `app/globals.css` contains `@import "tailwindcss"` and there is no `tailwind.config.js`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Project compiles, shadcn components present, all locked dependencies installed at pinned versions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Drizzle + Neon, define schema, and configure iron-session</name>
  <files>lib/db/index.ts, lib/db/schema.ts, lib/session.ts, drizzle.config.ts, .env.example</files>
  <read_first>
    - .planning/phases/01-foundation-auth-gate-student-roster/01-RESEARCH.md (Code Examples: drizzle.config.ts, "students + login_attempts schema", Pattern 4 neon-http driver)
    - .planning/phases/01-foundation-auth-gate-student-roster/01-CONTEXT.md (D-07 integer cents, D-10 archived flag, D-13 required parent email)
    - ./CLAUDE.md (Single Shared-Password Gate pattern — iron-session encrypted HttpOnly/Secure cookie; D-02 ~30-day TTL)
  </read_first>
  <action>
    Create `lib/db/schema.ts` exporting `students = pgTable("students", ...)` with columns: `id` serial primary key, `name` varchar(255) notNull, `rateCents` integer("rate_cents") notNull (D-07 — never a float/decimal column), `parentEmail` varchar("parent_email", 255) notNull (D-13 — required), `archived` boolean notNull default false (D-10 — soft delete), `createdAt` timestamp("created_at") notNull defaultNow. Also export `loginAttempts = pgTable("login_attempts", ...)` with `ipAddress` varchar("ip_address", 45) primary key, `failedCount` integer("failed_count") notNull default 0, `lockedUntil` timestamp("locked_until") nullable (AUTH-04 foundation). Create `lib/db/index.ts` exporting `db` built from `drizzle({ client: neon(process.env.DATABASE_URL!), schema })` using `drizzle-orm/neon-http` (the HTTP driver, NOT the WebSocket/pooled driver — correct for Vercel serverless). Create `lib/session.ts` exporting a `SessionData` interface `{ isLoggedIn: boolean }`, a `sessionOptions` object (cookieName `tutorinvoice_session`, password `process.env.SESSION_SECRET!`, cookieOptions: `httpOnly: true`, `secure: process.env.NODE_ENV === "production"`, `sameSite: "lax"`, `maxAge: 60*60*24*30` for the D-02 ~30-day TTL), and an async `getSession()` helper that returns `getIronSession<SessionData>(await cookies(), sessionOptions)`. Create `drizzle.config.ts` with `import "dotenv/config"` at the top, `defineConfig` pointing `schema: "./lib/db/schema.ts"`, `out: "./drizzle"`, `dialect: "postgresql"`, `dbCredentials.url: process.env.DATABASE_URL!`. Create `.env.example` documenting `DATABASE_URL`, `APP_PASSWORD`, `SESSION_SECRET` (with a note that SESSION_SECRET must be ≥32 chars). Verify `.env.local` is gitignored (create-next-app default).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'rate_cents' lib/db/schema.ts && grep -q 'login_attempts' lib/db/schema.ts && grep -q 'neon-http' lib/db/index.ts && grep -q 'sessionOptions' lib/session.ts</automated>
  </verify>
  <acceptance_criteria>
    - `lib/db/schema.ts` contains `export const students = pgTable("students"` with a `rate_cents` integer column, a notNull `parent_email` column, and an `archived` boolean defaulting to false
    - `lib/db/schema.ts` contains `export const loginAttempts = pgTable("login_attempts"` with `ip_address` primary key, `failed_count`, and nullable `locked_until`
    - `lib/db/index.ts` imports `drizzle` from `drizzle-orm/neon-http` and initializes with `neon(process.env.DATABASE_URL`
    - `lib/session.ts` exports `SessionData`, `sessionOptions` (httpOnly + secure-in-production + maxAge 2592000), and `getSession`
    - `drizzle.config.ts` begins with `import "dotenv/config"` and references `./lib/db/schema.ts`
    - `.gitignore` includes `.env*` (or `.env.local`) so secrets are never committed
  </acceptance_criteria>
  <done>Schema, db instance, session config, and drizzle config compile and reference the correct env vars and column shapes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Create Neon database and set secrets in .env.local</name>
  <what-built>Schema files and drizzle config are ready. Applying them to a live database requires a Neon Postgres database and the app's secrets — an external dashboard/account action Claude cannot perform.</what-built>
  <how-to-verify>
    1. Create a Neon Postgres database — easiest path: Vercel Dashboard -> Storage tab -> Neon integration (this auto-provisions and later auto-injects DATABASE_URL for the deploy in Plan 05). Alternatively create a project directly at the Neon console.
    2. Copy the Neon connection string.
    3. In the project root create `.env.local` (NOT committed) with three values:
       - `DATABASE_URL=` the Neon connection string
       - `APP_PASSWORD=` a single high-entropy shared password you will type to unlock the app (16+ chars recommended)
       - `SESSION_SECRET=` a random 32+ character string (generate with `openssl rand -base64 32`)
    4. Reply "ready" once `.env.local` contains all three values.
  </how-to-verify>
  <resume-signal>Type "ready" once `.env.local` has DATABASE_URL, APP_PASSWORD, and SESSION_SECRET set</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push schema to the live Neon database</name>
  <files>lib/db/schema.ts</files>
  <read_first>
    - .planning/phases/01-foundation-auth-gate-student-roster/01-RESEARCH.md (Pitfall 1 — push vs generate/migrate; use push for Phase 1)
    - drizzle.config.ts
    - lib/db/schema.ts
  </read_first>
  <action>
    [BLOCKING] Run `npx drizzle-kit push` to apply `lib/db/schema.ts` to the live Neon database referenced by `DATABASE_URL` in `.env.local`. This step is MANDATORY: `npx tsc --noEmit` and `next build` PASS without it because Drizzle types come from `schema.ts` at compile time, not from the live DB — skipping push creates a false-positive verification state where the app compiles but every query fails at runtime against a table that does not exist. If `drizzle-kit push` prompts interactively for a create/alter confirmation, accept the create of the `students` and `login_attempts` tables (first push against an empty DB — no destructive changes possible). Do NOT switch to `generate`/`migrate` this phase (Pitfall 1 — push is the Phase-1 choice; migrations start in Phase 2).
  </action>
  <verify>
    <automated>npx drizzle-kit push</automated>
  </verify>
  <acceptance_criteria>
    - `npx drizzle-kit push` exits 0
    - Querying the Neon DB shows a `students` table with columns id, name, rate_cents, parent_email, archived, created_at
    - Querying the Neon DB shows a `login_attempts` table with columns ip_address, failed_count, locked_until
  </acceptance_criteria>
  <done>The students and login_attempts tables exist in the live Neon database and match schema.ts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local dev → npm registry | Third-party packages are downloaded and executed at install/build time |
| app process → Neon Postgres | DATABASE_URL credential crosses into the DB connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | npm package installs | mitigate | RESEARCH.md "Package Legitimacy Audit" — every package scanned with slopcheck v0.6.1 on 2026-07-03, all `[OK]`/Approved, zero `[SLOP]`/`[SUS]`; versions pinned to the CLAUDE.md-locked, npm-verified set. No `[ASSUMED]` packages, so no per-install human checkpoint required. |
| T-01-01 | Information Disclosure | DATABASE_URL / APP_PASSWORD / SESSION_SECRET | mitigate | Secrets live only in `.env.local` (gitignored by create-next-app default) and are supplied via the human-action checkpoint; never hardcoded, never committed. `.env.example` documents names only, no values. |
| T-01-02 | Tampering | students.rate_cents money column | mitigate | Column is `integer` (cents), never float/decimal (D-07 / SESS-05); prevents floating-point currency drift at the storage layer. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (project compiles).
- shadcn components dialog/button/input/label/table present under components/ui/.
- `npx drizzle-kit push` exits 0 and the students + login_attempts tables exist in Neon.
- `.env.local` is gitignored; no secrets appear in tracked files.
</verification>

<success_criteria>
- Next.js 16 App Router project scaffolded with the exact locked stack versions.
- Drizzle+Neon wired via the neon-http driver; iron-session helper configured with a ~30-day HttpOnly/Secure cookie.
- students (integer-cents rate, required parent email, archived flag) and login_attempts tables live in Neon.
</success_criteria>

<artifacts_this_phase_produces>
New symbols/files created by THIS plan (source-grounding: these are new, not pre-existing drift):
- Files: package.json, tsconfig.json, next.config.ts, eslint.config.mjs, postcss.config.mjs, components.json, app/layout.tsx, app/globals.css, app/page.tsx (default placeholder), lib/utils.ts, lib/db/index.ts, lib/db/schema.ts, lib/session.ts, drizzle.config.ts, .env.example
- shadcn components: components/ui/dialog.tsx, button.tsx, input.tsx, label.tsx, table.tsx
- Schema tables: `students` (columns: id, name, rate_cents, parent_email, archived, created_at), `login_attempts` (columns: ip_address, failed_count, locked_until)
- Exports: `db` (lib/db/index.ts), `students` + `loginAttempts` (lib/db/schema.ts), `SessionData` + `sessionOptions` + `getSession` (lib/session.ts)
- Env vars introduced: DATABASE_URL, APP_PASSWORD, SESSION_SECRET
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/01-foundation-auth-gate-student-roster/01-01-SUMMARY.md` when done
</output>
