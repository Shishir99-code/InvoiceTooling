---
phase: 06-scheduled-invoice-generation
plan: 01
subsystem: database
tags: [drizzle, postgres, neon, schema, cadence]

requires:
  - phase: 03-invoice-generation
    provides: invoices + settings tables
  - phase: 05-recurring-class-schedules
    provides: single-push schema discipline, daily cron
provides:
  - settings.invoice_cadence_enabled / invoice_cadence_day / invoice_cadence_last_day / last_invoiced_month columns
  - invoices.auto_generated + invoices.sent columns
  - one-time backfill marking all pre-existing invoices sent=true
affects: [06-02-cadence-ui, 06-03-generation-core, 06-05-cadence-cron, 06-06-review-queue]

tech-stack:
  added: []
  patterns: [single additive schema push per phase, idempotent data backfill]

key-files:
  created: []
  modified: [lib/db/schema.ts]

key-decisions:
  - "All six Phase-6 columns added in one edit + one drizzle-kit push (mirrors Phase 5 Plan 01 single-push discipline)"
  - "sent backfill is a single idempotent UPDATE invoices SET sent=true so pre-feature invoices never flood the RINV-04 review queue (D-08)"

patterns-established:
  - "Cadence columns retained while disabled: invoice_cadence_day persists even when invoice_cadence_enabled=false (D-01)"
  - "invoice_cadence_day range 1–28 with explicit invoice_cadence_last_day boolean to avoid ambiguous 29/30/31 (D-02)"

requirements-completed: [RINV-01, RINV-02, RINV-04]

duration: 4min
completed: 2026-07-07
---

# Phase 6 Plan 01: Schema Foundation Summary

**Additive Neon push adding four `settings` cadence columns (enabled/day/last-day/last-invoiced-month) and two `invoices` review-queue columns (`auto_generated`, `sent`), with all pre-existing invoices backfilled to `sent=true`.**

## Performance

- **Duration:** ~4 min
- **Completed:** 2026-07-07
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `invoiceCadenceEnabled`, `invoiceCadenceDay`, `invoiceCadenceLastDay`, `lastInvoicedMonth` to the `settings` table.
- Added `autoGenerated`, `sent` (both NOT NULL default false) to the `invoices` table.
- Pushed to live Neon — additive only, no `--accept-data-loss` prompt, no FK/column changes.
- Ran the one-time `sent` backfill: `SELECT count(*) FROM invoices WHERE sent=false` now returns 0.

## Task Commits

1. **Task 1: Add cadence + review-queue columns, push, backfill** - `d628daa` (feat)

## Files Created/Modified
- `lib/db/schema.ts` - Six new columns across `settings` and `invoices`, matching existing snake_case / NOT-NULL-default conventions.

## Decisions Made
- None beyond the plan — single edit, single push, idempotent backfill exactly as specified.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required. (`CRON_SECRET` from Phase 5 is reused by Plan 05, not introduced here.)

## Next Phase Readiness
- Live schema now supports cadence config (Plan 02), auto/sent state writes (Plans 03/06), and the monthly high-water-mark guard (Plan 05).
- No further schema push required this phase.

---
*Phase: 06-scheduled-invoice-generation*
*Completed: 2026-07-07*
