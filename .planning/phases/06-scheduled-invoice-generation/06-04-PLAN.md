---
phase: 06-scheduled-invoice-generation
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - lib/validation/invoice.ts
  - lib/invoice/generate.ts
  - components/invoice-preview-dialog.tsx
autonomous: true
requirements: [RINV-03]
must_haves:
  truths:
    - "When generating manually, the tutor can optionally cap the invoice with a 'bill through [date]' cutoff; unbilled sessions dated after the cutoff are excluded from the frozen invoice."
    - "With no cutoff, manual generate still bills ALL of a student's currently-unbilled sessions (unchanged default)."
    - "The client preview total reflects the same cutoff the server applies, so what she sees is what freezes."
    - "The cadence/auto path is unaffected — it never passes a cutoff and always bills all unbilled sessions."
  artifacts:
    - path: "lib/validation/invoice.ts"
      provides: "invoiceGenerateSchema extended with optional throughDate"
      contains: "throughDate"
    - path: "lib/invoice/generate.ts"
      provides: "generateInvoiceForStudent honoring throughDate cutoff in the unbilled re-SELECT"
      contains: "throughDate"
    - path: "components/invoice-preview-dialog.tsx"
      provides: "optional through-date input + preview filtering"
      contains: "throughDate"
  key_links:
    - from: "components/invoice-preview-dialog.tsx"
      to: "generateInvoiceAction"
      via: "form field throughDate"
      pattern: "throughDate"
    - from: "lib/invoice/generate.ts"
      to: "sessions.date"
      via: "lte(sessions.date, throughDate) cutoff clause"
      pattern: "throughDate"
---

<objective>
Add the RINV-03 optional "bill through [date]" cutoff to the MANUAL generate flow only
(D-06): she can cap which unbilled sessions an invoice covers (e.g. bill this month, leave
next month's for later), while the default stays "all currently-unbilled sessions" and the
cadence/auto path always bills all unbilled. No start bound (rejected — unbilled sessions
already begin where the last invoice ended).

Wave 3 (parallel with Plan 05): depends on Plan 03's helper; touches the manual-generate
files that Plan 05 does not.

Purpose: give her a manual timeframe cap without changing the immutable-snapshot or
auto-cadence semantics.
Output: an optional through-date on the preview dialog, a `throughDate` on
`invoiceGenerateSchema`, and a cutoff clause in the shared helper's unbilled re-SELECT.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scheduled-invoice-generation/06-CONTEXT.md
@.planning/phases/06-scheduled-invoice-generation/06-RESEARCH.md
</context>

## Artifacts this phase produces (Plan 04)

New symbols/paths created here:
- `invoiceGenerateSchema.throughDate` (optional `yyyy-MM-dd` string)
- `generateInvoiceForStudent` cutoff clause `lte(sessions.date, throughDate)`
- through-date `Input` + preview filtering in `invoice-preview-dialog.tsx`

<tasks>

<task type="auto">
  <name>Task 1: Add optional throughDate to invoiceGenerateSchema and honor it in the helper</name>
  <files>lib/validation/invoice.ts, lib/invoice/generate.ts</files>
  <read_first>
    - lib/validation/invoice.ts (the file being modified — the current studentId-only schema and the T-03-04 "everything else is re-fetched server-side" comment)
    - lib/invoice/generate.ts (the `// Plan 04: throughDate cutoff` marker left in Plan 03, and the unbilled re-SELECT `where(and(eq(studentId), eq(billed,false)))` to extend)
    - lib/schedule/time.ts (date strings are "yyyy-MM-dd"; string comparison on the `date` mode:"string" column is valid — Pitfall 2)
    - .planning/phases/06-scheduled-invoice-generation/06-RESEARCH.md (F10 — cutoff is manual-only; keep it a yyyy-MM-dd string, no start bound)
  </read_first>
  <action>
    In `lib/validation/invoice.ts`: extend `invoiceGenerateSchema` with
    `throughDate: z.preprocess(v => (typeof v === "string" && v.trim() === "" ? undefined : v),
    z.string().regex(/^\d{4}-\d{2}-\d{2}$/, "Enter a valid date.").optional())` — blank → undefined
    (no cutoff), otherwise a `yyyy-MM-dd` string. Keep `studentId` unchanged.
    In `lib/invoice/generate.ts`: at the `// Plan 04: throughDate cutoff` marker, extend the unbilled
    re-SELECT `where` to `and(eq(sessions.studentId, studentId), eq(sessions.billed, false),
    opts.throughDate ? lte(sessions.date, opts.throughDate) : undefined)` (drizzle `and` ignores
    `undefined` operands, so no cutoff = all unbilled). Import `lte` from "drizzle-orm" alongside the
    existing operators. The rest of the helper (snapshot freeze, CTE, auto/sent) is unchanged — the
    CTE's `target`/`ins` still operate over exactly the sessionIds selected here, so the double-billing
    guard and period bounds automatically respect the cutoff.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -q "throughDate" lib/validation/invoice.ts && grep -q "lte(sessions.date" lib/invoice/generate.ts && echo CUTOFF_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `invoiceGenerateSchema` accepts a blank throughDate as undefined and a "2026-07-31" as a valid cutoff; a malformed value returns "Enter a valid date."
    - `generateInvoiceForStudent` applies `lte(sessions.date, throughDate)` only when a cutoff is present; with no cutoff it selects all unbilled sessions (default preserved).
    - The double-billing CTE still gates on exactly the selected sessionIds.
  </acceptance_criteria>
  <done>The shared helper caps the billed sessions at an optional through-date; the default remains all-unbilled.</done>
</task>

<task type="auto">
  <name>Task 2: Surface the optional through-date on the preview dialog and filter the preview</name>
  <files>components/invoice-preview-dialog.tsx</files>
  <read_first>
    - components/invoice-preview-dialog.tsx (the file being modified — the `sortedSessions` derivation, the `totalCents`/`period`/`invoiceBlock` preview computation, the hidden `studentId` input inside `<form action={formAction}>`, and the MAIL-05 popup handle logic that must stay intact)
    - components/ui/input.tsx (the Input primitive; a native `type="date"` Input is sufficient — no new dependency)
    - lib/validation/invoice.ts (the field name `throughDate` the form must submit)
  </read_first>
  <action>
    Add optional through-date state: `const [throughDate, setThroughDate] = useState("")`. Render an
    optional labeled `Input type="date"` (help text: "Optional — only bill sessions on or before this
    date. Leave blank to bill all unbilled sessions.") inside the existing `<form>`, and add a hidden
    field is NOT needed since the visible date Input can carry `name="throughDate"` directly (it
    submits its value; blank when untouched). Derive a `cutoffSessions` = `sortedSessions.filter(s =>
    !throughDate || s.date <= throughDate)` and compute `totalCents`, `period`, `invoiceBlock`, and the
    preview subject/body from `cutoffSessions` instead of `sortedSessions`, so the preview total and
    text update live as she picks a cutoff (mirrors the server filter — WYSIWYG). If `cutoffSessions`
    is empty, disable the submit button and show a hint ("No unbilled sessions on or before that
    date."). Do NOT touch the popupRef/MAIL-05 auto-open logic or the success-navigation effect.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint</automated>
    <automated>grep -q 'name="throughDate"' components/invoice-preview-dialog.tsx && grep -q "cutoffSessions" components/invoice-preview-dialog.tsx && echo PREVIEW_OK</automated>
    <automated>npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit`, `npm run lint`, and `npm run build` exit 0.
    - The dialog renders an optional `type="date"` input named `throughDate`; leaving it blank submits no cutoff.
    - Picking a cutoff recomputes the preview total/period/body from only the on-or-before sessions.
    - When the cutoff excludes every unbilled session, the submit is disabled with a hint.
    - The MAIL-05 draft auto-open and success navigation still work (untouched).
  </acceptance_criteria>
  <done>The manual generate dialog offers an optional through-date whose preview matches what the server will freeze.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` exit 0.
- Manual generate with a cutoff freezes only on-or-before-cutoff unbilled sessions; the preview total matches.
- Manual generate with no cutoff still bills all unbilled sessions (default unchanged).
- The cadence path passes no cutoff (verified in Plan 05) and is unaffected.
</verification>

<threat_model>
ASVS L1 — an optional field on an existing password-gated generate flow.
- **Input validation:** `throughDate` is server-validated (regex `yyyy-MM-dd`, optional) at the zod
  boundary before it reaches the SQL cutoff; a malformed value is rejected as a field error, never
  interpolated raw. The `lte(sessions.date, throughDate)` clause is a parameterized drizzle operand
  (no string concatenation into SQL).
- **Snapshot integrity preserved:** the cutoff only narrows the server-side re-SELECT; the frozen
  total/period/lineItems are still derived from stored `amountCents` over exactly the selected
  sessions, and the double-billing guard is unchanged — a cutoff cannot cause a partial or duplicate
  bill.
- **Client preview is non-authoritative:** the dialog filter is UX-only; the server re-applies the
  cutoff independently (never trusts the client's session list or total).
- **No new auth surface, no secret, no external call.**
</threat_model>
</content>
