---
phase: 04-quick-wins-auto-open-email-zoom-links-timezone
plan: 04
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/settings/timezones.ts
  - lib/validation/settings.ts
  - lib/actions/settings.ts
  - components/settings-form.tsx
  - app/(app)/settings/page.tsx
autonomous: true
requirements: [SET-03]
must_haves:
  truths:
    - "User can set her local (IANA) timezone in Settings from a US-timezone shortlist; the control defaults to the stored zone, or the browser-detected zone on first visit."
    - "The chosen timezone persists across a page reload and is stored as an IANA string (e.g. America/New_York) on the single-row settings table."
    - "A non-IANA timezone value is rejected server-side with an inline error; a blank (untouched) value does not error."
    - "Phase 4 does NOT consume the timezone in any calculation — it is captured only (for Phase 5/6)."
  artifacts:
    - path: "lib/settings/timezones.ts"
      provides: "US_TIMEZONES shortlist + isValidIanaTimeZone helper"
      contains: "isValidIanaTimeZone"
    - path: "lib/validation/settings.ts"
      provides: "optional IANA-validated timezone field"
      contains: "timezone"
    - path: "components/settings-form.tsx"
      provides: "timezone Select with browser-detect default + hidden submit input"
      contains: "timezone"
  key_links:
    - from: "lib/validation/settings.ts"
      to: "isValidIanaTimeZone"
      via: "refine in settingsFormSchema"
      pattern: "isValidIanaTimeZone"
    - from: "components/settings-form.tsx"
      to: "US_TIMEZONES"
      via: "import from @/lib/settings/timezones"
      pattern: "US_TIMEZONES"
    - from: "lib/actions/settings.ts"
      to: "settings.timezone"
      via: "upsert values/set timezone"
      pattern: "timezone"
---

<objective>
Deliver SET-03: the tutor sets her local IANA timezone in Settings, stored as a string on
the single-row settings table. Auto-detect the browser zone as the default with a US
shortlist to override (D-11). Phase 4 only **captures** it — no calculation consumes it
here; Phase 5 (class-day resolution) and Phase 6 (invoice cadence) are the consumers.

Depends on Plan 01 (the `settings.timezone` column must exist in the live DB). Runs in
parallel with Plan 03 (Zoom) — disjoint files.

Output: a validated, persisted timezone captured via the Settings form.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quick-wins-auto-open-email-zoom-links-timezone/04-CONTEXT.md
@.planning/phases/04-quick-wins-auto-open-email-zoom-links-timezone/04-RESEARCH.md
</context>

## Artifacts this phase produces (Plan 04)

New symbols/paths created here (excluded from source-grounding drift checks):
- File `lib/settings/timezones.ts` — `US_TIMEZONES` (`{value,label}[]`), `isValidIanaTimeZone(tz)`
- `timezone` field in `settingsFormSchema` (`lib/validation/settings.ts`)
- `timezone` handling in `saveSettingsAction` (`lib/actions/settings.ts`)
- `timezone` Select + hidden input in `components/settings-form.tsx`
- `timezone` prop passed from `app/(app)/settings/page.tsx`

<tasks>

<task type="auto">
  <name>Task 1: US timezone shortlist + IANA validator</name>
  <files>lib/settings/timezones.ts</files>
  <read_first>
    - lib/invoice/defaults.ts (the "pure module, no React/DB import" convention to mirror for a new lib file)
    - .planning/phases/04-quick-wins-auto-open-email-zoom-links-timezone/04-RESEARCH.md (§3 — shortlist entries + the try/catch isValidIanaTimeZone helper)
  </read_first>
  <action>
    Create a pure module (no React/DB import). Export
    `export const US_TIMEZONES: { value: string; label: string }[]` with:
    America/New_York (Eastern), America/Chicago (Central), America/Denver (Mountain),
    America/Los_Angeles (Pacific), America/Phoenix (Arizona), America/Anchorage (Alaska),
    Pacific/Honolulu (Hawaii). Export
    `export function isValidIanaTimeZone(tz: string): boolean { try { new
    Intl.DateTimeFormat("en-US", { timeZone: tz }); return true; } catch { return false; } }`
    (Node 20 throws RangeError for unknown zones — RESEARCH §3).
  </action>
  <acceptance_criteria>
    - `US_TIMEZONES` is a non-empty `{value,label}[]` including `America/New_York`.
    - `isValidIanaTimeZone("America/New_York")` is true; `isValidIanaTimeZone("Not/AZone")` is false.
    - Module has no React/DB imports; `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Reusable timezone shortlist + validator exist.</done>
</task>

<task type="auto">
  <name>Task 2: Optional IANA-validated timezone in the settings schema</name>
  <files>lib/validation/settings.ts</files>
  <read_first>
    - lib/validation/settings.ts (the file being modified — `.pick({...})` selects submitted fields; mirror the trim/max validation style)
    - lib/settings/timezones.ts (isValidIanaTimeZone from Task 1)
  </read_first>
  <action>
    Import `isValidIanaTimeZone` from `@/lib/settings/timezones`. Chain a `.extend({...})`
    onto the existing `settingsFormSchema` `.pick({...})` (do NOT add `timezone: true` to the
    `.pick` — that would inherit createInsertSchema's plain nullable string and skip the IANA
    check). Add a `timezone` field: optional, blank allowed, and when present a recognized
    IANA zone. Use:
    `timezone: z.preprocess((v) => (typeof v === "string" && v.trim() === "" ? undefined : v),
      z.string().refine(isValidIanaTimeZone, "Pick a valid timezone.").optional())`.
    Keep zelleHandle/subjectTemplate/bodyTemplate validation unchanged.
  </action>
  <acceptance_criteria>
    - `settingsFormSchema` includes `timezone`, optional, empty→undefined, refined by `isValidIanaTimeZone`.
    - An unknown zone fails parse; `America/Chicago` passes; blank passes.
    - `npx tsc --noEmit` exits 0; existing three fields' validation unchanged.
  </acceptance_criteria>
  <done>Server-side timezone validation exists, optional and IANA-checked.</done>
</task>

<task type="auto">
  <name>Task 3: Persist timezone in the settings upsert</name>
  <files>lib/actions/settings.ts</files>
  <read_first>
    - lib/actions/settings.ts (the file being modified — `parseSettingsForm` reads named fields; the single-row `onConflictDoUpdate` upsert sets each column in BOTH values and set)
    - lib/validation/settings.ts (the schema now exposing timezone)
  </read_first>
  <action>
    Add `timezone: formData.get("timezone")` to `parseSettingsForm`'s parsed object. In the
    upsert, add `timezone: parsed.data.timezone ?? null` to BOTH the `.values({ id: 1, ... })`
    object and the `onConflictDoUpdate({ set: { ... } })` object (store NULL when blank).
    Keep `revalidatePath("/settings")` and the other fields unchanged.
  </action>
  <acceptance_criteria>
    - `parseSettingsForm` reads `timezone`; both the insert values and the conflict `set` write `timezone: parsed.data.timezone ?? null`.
    - No change to the zelle/subject/body upsert fields or revalidatePath.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The chosen timezone saves and updates on the single settings row.</done>
</task>

<task type="auto">
  <name>Task 4: Timezone Select in the Settings form</name>
  <files>components/settings-form.tsx</files>
  <read_first>
    - components/settings-form.tsx (the file being modified — add a new SettingsFormProps field `timezone: string | null`; mirror the existing field-block layout + fieldErrors paragraph)
    - components/session-form-dialog.tsx (the shadcn Select usage pattern: value/onValueChange + a hidden input to submit the value with the form action)
    - lib/settings/timezones.ts (US_TIMEZONES)
    - components/ui/select.tsx (Select/SelectTrigger/SelectValue/SelectContent/SelectItem API)
  </read_first>
  <action>
    Add `timezone: string | null` to `SettingsFormProps`. Import the shadcn Select parts and
    `US_TIMEZONES`. Compute the default selection client-side:
    `const detected = Intl.DateTimeFormat().resolvedOptions().timeZone;` then
    `const initialTz = props.timezone ?? detected;`. Build the option list from `US_TIMEZONES`,
    and if `initialTz` is NOT already in it, prepend `{ value: initialTz, label: initialTz +
    " (detected)" }` so her real zone is selectable (RESEARCH §3). Track selection with
    `useState(initialTz)` and render a labeled `Select` (Label "Timezone", a short helper "Used
    later to schedule classes and time invoices."). Mirror the chosen value into a
    `<input type="hidden" name="timezone" value={tz} />` so it submits with the existing
    `<form action={formAction}>`. Add the `state.fieldErrors?.timezone` error paragraph. Keep
    the Save button, Saved flash, and the three existing fields unchanged.
  </action>
  <acceptance_criteria>
    - The form renders a timezone `Select` populated from `US_TIMEZONES`, defaulting to `props.timezone ?? Intl.DateTimeFormat().resolvedOptions().timeZone`, with the detected zone injected as an option when not in the shortlist.
    - A `<input type="hidden" name="timezone">` carries the selected value into the submit.
    - `state.fieldErrors?.timezone` renders when present; the other three fields and Saved flash are unchanged.
    - `npx tsc --noEmit` + `npm run lint` exit 0.
  </acceptance_criteria>
  <done>The tutor can pick/confirm her timezone in Settings.</done>
</task>

<task type="auto">
  <name>Task 5: Pass the stored timezone into the form</name>
  <files>app/(app)/settings/page.tsx</files>
  <read_first>
    - app/(app)/settings/page.tsx (the file being modified — it reads the settings row and passes zelleHandle/subjectTemplate/bodyTemplate props with `?? DEFAULT_*` fallbacks)
  </read_first>
  <action>
    Pass `timezone={row?.timezone ?? null}` to `<SettingsForm />` (NULL when unset — the form
    then prefills the browser-detected zone client-side per D-11). Leave the other three props
    and their default fallbacks unchanged.
  </action>
  <acceptance_criteria>
    - `<SettingsForm>` receives `timezone={row?.timezone ?? null}`.
    - The other three props are unchanged; `npx tsc --noEmit` + `npm run lint` + `npm run build` exit 0.
  </acceptance_criteria>
  <done>The stored timezone (or null) reaches the form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings form → saveSettingsAction | timezone crosses as free text; server zod + isValidIanaTimeZone is the sole gate (noValidate form) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering (invalid/injected zone) | timezone input | mitigate | Server-side `isValidIanaTimeZone` (Intl RangeError check) rejects any non-recognized string before storage, so downstream Phase 5/6 date math can trust the column |
| T-04-12 | Tampering (oversize) | timezone value | mitigate | Column is varchar(64); longest IANA zone is well under that; a non-IANA over-length value is rejected by the refine, not a raw Postgres error |
| T-04-13 | Information Disclosure | Settings page | accept | Behind Phase 1 auth gate; single-user app, no cross-tenant data |
</threat_model>

<verification>
- `npx tsc --noEmit`, `npm run lint`, `npm run build` pass.
- Source assertions: `isValidIanaTimeZone` used in settings validation; upsert writes timezone in both values and set; form has a `name="timezone"` hidden input fed by the Select; settings page passes `row?.timezone ?? null`. No timezone-consuming calculation added anywhere this phase.
- End-of-phase human check: open Settings on a fresh row → the control shows the browser-detected zone → pick America/Chicago → Save → reload → America/Chicago persists.
</verification>

<success_criteria>
- SET-03: user can set her local IANA timezone (auto-detected default + US shortlist), stored as a string; captured only, no consumption this phase.
</success_criteria>

<output>
Create `.planning/phases/04-quick-wins-auto-open-email-zoom-links-timezone/04-04-SUMMARY.md` when done.
</output>
