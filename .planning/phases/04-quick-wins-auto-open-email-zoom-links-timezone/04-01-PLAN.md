---
phase: 04-quick-wins-auto-open-email-zoom-links-timezone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/db/schema.ts
autonomous: true
requirements: [ZOOM-01, SET-03]
must_haves:
  truths:
    - "The students table has a nullable zoomLink column and the settings table has a nullable timezone column in the LIVE Neon database after schema push."
    - "Existing student and settings rows are unaffected by the push (nullable adds, no default backfill, no destructive change)."
    - "The existing onDelete: restrict FKs on sessions/invoices are unchanged."
  artifacts:
    - path: "lib/db/schema.ts"
      provides: "students.zoomLink (nullable varchar) + settings.timezone (nullable varchar)"
      contains: "zoomLink"
  key_links: []
---

<objective>
Lay the entire schema foundation for Phase 4 in one place so the `drizzle-kit push`
happens exactly once: add a nullable `zoomLink` column to `students` (ZOOM-01) and a
nullable `timezone` column to `settings` (SET-03), then push to the live Neon DB.

Both are additive **nullable** columns — safe on push against production data (existing
rows get NULL, no default backfill). Wave 2 plans (ZOOM send, timezone form) build
against the already-pushed schema with no further push. MAIL-05 (Plan 02) needs no
schema change and runs in parallel in Wave 1.

Output: live `students.zoom_link` + `settings.timezone` columns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-quick-wins-auto-open-email-zoom-links-timezone/04-CONTEXT.md
@.planning/phases/04-quick-wins-auto-open-email-zoom-links-timezone/04-RESEARCH.md
</context>

## Artifacts this phase produces (Plan 01)

New symbols/paths created here (excluded from source-grounding drift checks):
- Column `students.zoomLink` → `zoom_link` (nullable `varchar(512)`)
- Column `settings.timezone` → `timezone` (nullable `varchar(64)`)

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1 [BLOCKING]: Add students.zoomLink + settings.timezone, then push schema</name>
  <files>lib/db/schema.ts</files>
  <read_first>
    - lib/db/schema.ts (the file being modified — copy its comment-annotated, explicit-column style verbatim; note students uses varchar(255) for name/parentEmail and settings uses a single-row id=1 pattern)
    - drizzle.config.ts (confirms push reads DATABASE_URL from .env.local via dotenv)
    - .planning/phases/04-quick-wins-auto-open-email-zoom-links-timezone/04-RESEARCH.md (§4 Migration mechanics — nullable adds are non-destructive on push)
  </read_first>
  <action>
    Make BOTH column additions in ONE edit so a single push covers them:
    1. In `export const students`, add after `parentEmail`:
       `zoomLink: varchar("zoom_link", { length: 512 })` — NULLABLE (no `.notNull()`,
       no `.default()`; blank/absent allowed per D-05). Add a short comment: `// ZOOM-01:
       optional per-student Zoom link; nullable`.
    2. In `export const settings`, add after `bodyTemplate`:
       `timezone: varchar("timezone", { length: 64 })` — NULLABLE (no `.notNull()`, no
       `.default()`). Add a comment: `// SET-03: IANA zone captured for Phase 5/6; nullable
       so the form can prefill the browser-detected zone (D-11/D-12)`.
    Do NOT touch the sessions/invoices/loginAttempts tables or any existing FK/onDelete.
    Then run the schema push against the live DB.
  </action>
  <acceptance_criteria>
    - lib/db/schema.ts `students` contains `zoomLink: varchar("zoom_link"` and it is NOT `.notNull()`.
    - lib/db/schema.ts `settings` contains `timezone: varchar("timezone"` and it is NOT `.notNull()`.
    - No change to sessions/invoices FKs (`onDelete: "restrict"` / `"set null"` still present, byte-for-byte).
    - `npx drizzle-kit push` completes without an interactive prompt and reports the two columns added (if it prompts and cannot proceed non-interactively, stop and flag for manual run — this task is then not autonomous).
    - `npx tsc --noEmit` exits 0 (Drizzle infers the new nullable string|null fields on `$inferSelect`).
  </acceptance_criteria>
  <done>Both nullable columns exist in the live Neon DB and typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema push → live Neon DB | Additive nullable columns only; no data migration, no destructive op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (data loss) | drizzle-kit push on prod | mitigate | Both changes are additive nullable adds — push cannot drop/rename an existing column; existing rows untouched |
| T-04-02 | DoS (bad migration) | push blocking on prompt | accept | Nullable adds do not require a default; push is non-interactive. Task flagged non-autonomous if a prompt appears |
</threat_model>

<verification>
- `npx tsc --noEmit` and `npm run lint` pass.
- Source assertions: `zoom_link` and `timezone` columns present and nullable in schema.ts; no diff to existing FK lines.
- Live-DB check: after push, the columns exist (drizzle-kit push output, or a `\d students` / `\d settings` inspection).
</verification>

<success_criteria>
- ZOOM-01 (schema half): students carries a nullable zoomLink column.
- SET-03 (schema half): settings carries a nullable timezone column.
</success_criteria>

<output>
Create `.planning/phases/04-quick-wins-auto-open-email-zoom-links-timezone/04-01-SUMMARY.md` when done.
</output>
